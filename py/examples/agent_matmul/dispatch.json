{
  "_comment": "rule -> threadgroup total threads; grid_rule -> grid total threads. Names in scope: M N K ceildiv max min simd tg. Naive baseline: one thread per output element, 256-thread TGs.",
  "rule": "256",
  "grid_rule": "ceildiv(M*N, 256) * 256"
}
