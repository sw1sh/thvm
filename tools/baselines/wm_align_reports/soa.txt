soa (ShefferAxiomsOrAssociativity) WM-alignment diagnosis -- firstdiv=125

## DEFINITIVE 2026-06-20: needs the global CP-formation reorder (local fix PROVEN impossible)

thvm's backward subsumption is ALREADY WM-faithful: atp_eset_subsume_by_new
(src/atp/_.c, ON in the WALDMEISTER preset, KBO_UN-gated, orphans victims, skips
oriented rules) == WM RueckwaertsSubsumierung (Interreduktion.c:251, syntactic
one-sided MO_TermpaarSubsummiertZweites under !TP_TermpaarIstRegel).  The
THVM_ATP_BWD_SUB knob (atp_push_rule:8675) is a DIFFERENT Vampire-style
over-firing mechanism -- the WRONG one, correctly OFF (it diverges soa @10).
pick-125 cannot be flipped locally: slot15 (C3 V0 (C3 V1 V0))=(C3 (C3 V1 V1) V0)
and eqn-10 (C3 V0 (C3 V1 V0))=(C3 V0 (C3 V1 V1)) share an LHS, differ by ONE
top-commutativity RHS swap; the plain subsumption predicate (correctly) bails.
Three WM-faithful comm-aware widenings that DO subsume slot15 all forked the soa
prefix at pick-99: removing slot15 suppresses formation of the commutativity CP
(C3 V0 V1)=(C3 V1 V0) that pick-99 selects, because thvm's slot15 is the UNIQUE
parent of that CP copy while WM produces an equivalent copy from a DIFFERENT live
parent at the right FIFO age.  => CP-formation-GRAPH divergence (which parent
pair yields the surviving duplicate-CP copy) + the cross-rule weight-120 FIFO-age
interleave = the global reorder.  No local subsumption/orientation predicate
fixes it.  (Verified workflow wf_5e7ce207.)

CONFIG (opt-in):
  THVM_ATP_CP_WM_SIDE=1 THVM_ATP_WM_FLAT_SUBSUME=1 THVM_ATP_WALDMEISTER=1
  bin/test_atp_wolfram_bench /tmp/soa.pr <budget> <maxw>
RESULT: prefix=124 identical=N firstdiv=125; thvm pop-set superset of WM's;
        weights-exact through the aligned prefix.

THE pick-125 FACT (thvm side):
  thvm selects at weight 120 in pure formation-age (cp_seq) FIFO order.
  Picks 123,124 = the equation copies cp_seq 562,563
    opCenterdot(opCenterdot(v1,opCenterdot(v1,v1)),v2) # opCenterdot(v2,v2)
    == (C3 (C3 V0 (C3 V0 V0)) V1) # (C3 V1 V1).
  Pick 125 = cp_seq 564
    (C3 (C3 V0 V0) V1) # (C3 (C3 V0 V1) V1)
    == opCenterdot(opCenterdot(v1,v1),v2) # opCenterdot(opCenterdot(v1,v2),v2).
  thvm's NEXT equation copy after 563 is cp_seq 593 (then 613); the seqs
  between (564 seq564-form, 565 a distinct w=120 CP) are NOT the equation.

DOES WM FORM seq=564?  YES -- the prior "appears NOWHERE in WM's trace"
claim is WRONG (it was a grep miss: WM splits lhs/rhs across the `#`
multiline form and renames vars).  GREP EVIDENCE (banked /tmp/soa_align):
  wm.txt:5602  critical pair 991 (parents -10 and 13):
       opCenterdot(opCenterdot(x1,x1),x2) # opCenterdot(x2,opCenterdot(x2,opCenterdot(x2,opCenterdot(x1,x2))))
    ... normalized to:
       opCenterdot(opCenterdot(x1,x1),x2) # opCenterdot(opCenterdot(x1,x2),x2)
    ... added to SUE: 120, 659.
  This normalized form == thvm seq=564 (alpha-equal).  WM forms it 12x
  (wm.txt:5604,6772,41084,41567,42364,42750,43244,43879,44475,45001,...),
  ALWAYS weight 120, first SUE age 659, later 5046/5206/5304/5454/9047.
  WM NEVER selects it (no "added as new rule" with this form): it sits
  in SUE forever, buried behind lower-age weight-120 copies.

ROOT CAUSE (decisively re-diagnosed; pure AGE-INTERLEAVE at weight 120):
  Both engines queue (a) the equation and (b) seq=564 at weight 120.
  The divergence is the RELATIVE FORMATION AGE of seq=564's first copy
  vs the equation's 3rd copy:
    WM : eqn ages 222, 511, 577 ; seq564 first age 659
         -> 3 eqn copies precede seq564 -> WM picks 123,124,125 = eqn,
            seq564 waits.
    thvm: eqn cp_seq 562, 563     ; seq564 cp_seq 564  ; next eqn 593
         -> only 2 eqn copies precede seq564 -> thvm picks 123,124 = eqn,
            125 = seq564.
  thvm is MISSING one equation copy in the (563, 593) age window that WM
  has at age 577.  WM forms ~33912 CPs and queues the equation 227x at
  spread-out ages (222/511/577/1213/...); thvm forms fewer, with the
  equation's copies landing at cp_seq 167/192/235/.../562/563/593/613.
  The first 124 weight-ordered selections coincide; at the 125th the
  per-copy age drift finally swaps a weight-120 selection.

THE TASK'S HYPOTHESIZED LEVER (pop-time AC-perm-subsume) IS A DEAD END --
three independent disproofs:
  1. seq=564 is NOT AC-redundant.  lhs (C3 (C3 V0 V0) V1) and rhs
     (C3 (C3 V0 V1) V1) are genuinely distinct terms (inner V0,V0 vs
     V0,V1); they are NOT a commutativity/AC permutation of each other.
     atp_cp_perm_subsumed correctly returns 0 on it.
  2. opCenterdot (Sheffer NAND) is NOT an AC operator.  auto_ac on the
     soa axioms reports label 3: C=0 A=0 (THVM_ATP_AUTO_AC_DEBUG ->
     mask=0x0).  The perm-subsume mask is empty; the WM-faithful filter
     is structurally inert here (run stats: perm-subsumed=0).
  3. Even if seq=564 were dropped, pick-125 would become cp_seq 565
     ((C3 V0 V0) # (C3 (C3 V1 (C3 V1 V1)) V0)) -- ANOTHER non-equation
     w=120 CP -- NOT the equation.  The equation's 3rd thvm copy is
     cp_seq 593, ~29 ages later.  No pop-time drop of seq=564 can
     promote the equation; only forming a 3rd equation copy in (563,593)
     would, which is the CP-FORMATION-ORDER subsystem, not a filter.
  Empirical confirmation: forcing the mask on (THVM_ATP_PERM_SUB=1
  THVM_ATP_PERM_SUB_MASK=3) drops just 1 unrelated commutativity CP
  (perm-subsumed=1) AND shatters the whole alignment (pick-123 jumps to a
  pri=224 CP, prefix 1..124 broken) -- the AC-saturation-on-a-non-AC-top
  blow-up MEMORY warns about.

VERDICT: DEFERRED -- CP-formation age/multiplicity class, NOT a filter
class.  WM's seq=564 (== thvm's) is a real shared CP both engines queue
at weight 120; the only difference is WM forms a 3rd equation copy (age
577) in the window where thvm forms none, so WM's seq=564 (age 659) loses
the pick-125 race that thvm's seq=564 (cp_seq 564) wins.  Closing soa past
124 requires matching WM's CP-FORMATION ORDER + duplicate MULTIPLICITY
(the atp_overlap_ij enumeration order producing one more equation copy in
the critical age window), exactly the subsystem the prior wmo_dfs verdict
isolated.  A pop-time perm-subsume is the WRONG TOOL: there is no
AC-redundant CP to drop here, and dropping seq=564 promotes seq=565, not
the equation.

================================================================================
A-vs-B RE-DIAGNOSIS (2026-06-20): NOT (A) missing-enumeration; NOT a
re-overlap either.  Pure intra-rule overlap-ORDER at weight 120.  The
"thvm forms only 2 E copies / MISSES the age-577 copy" premise above is
WRONG (it counted only the (563,593) window, ignoring 15 earlier E copies).
--------------------------------------------------------------------------------
THE THREE WM E-COPIES' PARENTS (CONFIRMED, /tmp/soa_align/wm.txt):
  age 222  CP287  parents (-7, -5)  -- E-eqn-7 [x.x = (z.(z.z)).x]
                                       x E-eqn-5 [commutativity]
  age 511  CP774  parents (11, -7)  -- rule 11 [x.(x.(z.x)) -> z.x]
                                       x E-eqn-7
  age 577  CP876  parents (13, -6)  -- rule 13 [x.(x.(x.z)) -> z.x]
                                       x E-eqn-6 [x.x = x.(z.(z.z))]
  All THREE are DISTINCT (parents,pos) superpositions, each fired ONCE in
  WM; the age-577 copy is a fresh superposition, NOT a re-overlap of
  222/511.  (WM's seq564-equiv = CP991 parents (-10, 13) age 659.)

THVM ENUMERATES ALL THREE -- including the exact age-577 (rule13 x eqn6):
  thvm slot map by TERM (THVM_ATP_RULE_TRACE): slot19 == WM rule13
  [(C3 V0 (C3 V0 (C3 V0 V1)))->(C3 V1 V0)]; slot7 == WM eqn6-reversed
  [(C3 V0 (C3 V1 (C3 V1 V1)))->(C3 V0 V0)]; slot5 == commutativity;
  slot8 == E-as-rule [(C3 (C3 V0 (C3 V0 V0)) V1)->(C3 V1 V1)].
  THVM_ATP_CPGEN_DEBUG (parent-TERM provenance) + THVM_ATP_CP_FORM_TRACE
  interleaved (/tmp/soa_align/thvm_both.txt) pin the heap copies:
    cp_seq 562 = slot19 x slot8  (rule13 x E-rule)         -> E
    cp_seq 563 = slot19 x slot7  (rule13 x eqn6)           -> E  <== the
                  EXACT analog of WM age-577 (rule13 x eqn6); thvm forms
                  it at 563, NOT in (563,593), NOT zero.
    cp_seq 564 = slot19 x slot15/20 (rule13 x other)       -> seq564
    cp_seq 593 = slot20 x slot3                            -> E
  thvm produces E from ~95 distinct (parent-pair) overlaps in the budget-700
  cpgen; it OVER-produces E vs WM (18 heap copies by cp_seq 593 vs WM's 3
  by age 659).  There is NO superposition WM enumerates that thvm forms
  zero times.  => the task's case (A) is FALSE.

THE REAL DIVERGENCE (rule-13 / slot-19 internal overlap ORDER):
  WM fires rule13's overlaps spread out: rule13 x eqn6 -> E at age 577;
  rule13 x (-10) -> seq564 at age 659.  So WM's seq564 lands 82 ages AFTER
  its 3rd E copy -> WM picks E at 125.
  thvm fires slot19's overlaps consecutively at slot19's birth (rules=22):
  slot19 x slot8 -> E (562), slot19 x slot7 -> E (563), slot19 x slot15 ->
  seq564 (564).  So thvm's seq564 lands 1 age AFTER its (locally) 2nd E copy
  -> thvm picks seq564 at 125.
  Both engines enumerate the identical overlap SET; they differ only in the
  RELATIVE EMISSION ORDER of one rule's (rule13/slot19) E-overlaps vs its
  seq564-overlap, and in WM interleaving the seq564 copy from a DIFFERENT
  parent (-10,13) at a much later age (659) than thvm's same-rule slot19
  emission (564).

VERDICT (FINAL): NOT (A) missing-enumeration (thvm forms the age-577
rule13xeqn6 overlap at cp_seq 563), NOT (B) re-overlap-multiplicity (WM's
three E copies are three distinct one-shot superpositions, none repeated).
The residual is the atp_overlap_ij / cp_visit EMISSION-ORDER within a
single rule's overlap batch: thvm emits rule13's two E-overlaps (x slot8,
x slot7) before its seq564-overlap (x slot15), so seq564 immediately
follows the E pair at the same rule-birth tick; WM's heap-age assignment
interleaves a non-rule13 seq564 producer (-10,13) far later.  Matching
pick-125 requires reproducing WM's per-rule CP emission order + WM's
cross-rule age interleave at weight 120 -- the CP-FORMATION-ORDER subsystem,
unchanged conclusion (DEFERRED) but with the precise mechanism now pinned:
it is an overlap-emission-ORDER question, not a missing CP and not a filter.

REGRESSION (this re-diagnosis is documentary; no source change):
  git diff empty; bin/test_atp green 136232/136232; pick-seq for soa
  prefix 1..124 unchanged (no code touched).

================================================================================
SLOT15-vs-EQN10 FORK RESOLVED (2026-06-20): CASE (ii) OVER-ENUMERATION, and
the only mechanism that flips pick-125 to E is GLOBAL (trades the prefix).
--------------------------------------------------------------------------------
THE FORK (was: is thvm's seq564 partner slot15 == WM's seq564 partner eqn-10?).
  thvm seq564 producer (cp_seq 564, FIRST copy; pinned by [cpgen] norm-match,
  thvm_both.txt:1687): outer=slot19[=WM rule13] x inner=slot15, combo=0.
  thvm slot15 = (C3 V0 (C3 V1 V0)) -> (C3 (C3 V1 V1) V0)        [unorientable]
  WM eqn-10   = (C3 V0 (C3 V1 V0))  = (C3 V0 (C3 V1 V1))        [wm.txt:5129]
  SAME LHS, DIFFERENT RHS.  They are NOT the same equation.  thvm's slot20
  (= (C3 V0 (C3 V1 V0)) -> (C3 V0 (C3 V1 V1)), thvm_rt slot20) IS eqn-10.
  => CASE (ii): thvm forms seq564 from rule13 x slot15, a (parent-pair) WM
     never performs -- WM has NO slot15-as-rule.

WHY thvm has slot15 as a rule and WM does not (the over-enum source):
  WM NEVER orients slot15's term into a rule and NEVER numbers it in E:
  at wm.txt:5684 WM TOUCHES the slot15-term CP
  `opCenterdot(x1,opCenterdot(x2,x1)) # opCenterdot(opCenterdot(x2,x2),x1)`
  and reports "... subsumed." -- WM joins/subsumes it (via eqn-10 added at
  5129 + commutativity) so it never enters R or E.  thvm instead SELECTS it
  (pick-121) and installs slot15 as an active oriented rule, then overlaps it.
  ORDER DEPENDENCE: thvm derives slot15 (thvm_rt slot15) BEFORE slot20/eqn-10
  (thvm_rt slot20); at slot15's birth eqn-10 does not exist so thvm cannot
  subsume it.  WM derives eqn-10 first and uses it to kill the slot15-term.

NO LOCAL FILTER (pop-time drop of seq564 promotes seq565, not E -- already
established).  And the 17-E-copies recount: thvm forms E at cp_seq 167/192/
235/236/238/246/270/293/316/340/396/436/475/493/540/562/563/[564=seq564]/593...
(17 E copies BEFORE 564, not 2) and SELECTS E at picks 14/17/24/25/26/28/31/
34/37/40/.../123/124.  pick-125 is therefore a PURE weight-120 FIFO interleave
over the WHOLE run's formation-age stream, not a 2-vs-3 local count.

THE ONLY KNOB THAT YIELDS E@125 IS GLOBAL (decisive empirical trade proof,
WALDMEISTER preset, /tmp/soa.pr 300 40, real-selection stream excl. ORPHAN):
  baseline                 pick125 = seq564   prefix1..124 baseline
  THVM_ATP_BWD_SUB=1       pick125 = E (!)    but FIRST DIVERGES AT pick-10
                                              (pri 48 vs 109) -- whole stream
                                              reshuffled; picks 118..124 all
                                              differ.  This IS WM's backward
                                              subsumption (the mechanism that
                                              removes slot15-term + collapses
                                              E-dups); it reproduces WM's
                                              pick-125 ONLY by reproducing WM's
                                              entire weight-120 formation order.
  THVM_ATP_BWD_DEMOD=1     pick125 = seq564   prefix SAME  (no effect on 125)
  THVM_ATP_BWD_GROUND_JOIN=1 pick125 = seq564 prefix SAME  (no effect)
  THVM_ATP_FWD_SUB=1       pick125 = seq564   prefix SAME  (no effect)
  THVM_ATP_ESET_SUBSUME=0  diverges @10       THVM_ATP_WM_DEMOTE=0 diverges @10
  THVM_ATP_WM_EMISSION_ORDER=0 diverges @16   THVM_ATP_POP_SUBSUME=0 diverges @16
  THVM_ATP_IMPLICIT_CP=1   pick125 != E, prefix DIFF (other trajectory)

CONCLUSION (DEFERRED, with the global trade now PROVEN): every prefix-
preserving toggle leaves pick-125 = seq564; the single toggle that lands E at
125 (BWD_SUB = WM's RueckwaertsSubsumierung) reorders the selection stream from
pick-10 onward.  Flipping soa-125 = forming WM's seq564 from eqn-10 x rule13 at
WM's late age = adopting WM's backward-subsumption-driven formation order
WHOLESALE, which trades the 1..124 match for the 125 match.  No baseline-
preserving local fix exists; the residual genuinely requires the global CP-
formation-ORDER rewrite (matching WM's per-rule emission order AND its
backward-subsumption-driven cross-rule weight-120 age interleave).  Same shape
would help Meredith only via that same global reorder, not a local gate.

REGRESSION (this resolution is documentary; no source change):
  git diff empty; bin/test_atp green 136232/136232; SKIToBCKW c1 @1868 intact
  (pick-1868 = (C10 C8 (C10 C7 C5))->..., C8/W head); all baseline pick-seq
  md5 unchanged by construction (no code touched).
