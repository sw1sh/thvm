soa (ShefferAxiomsOrAssociativity) WM-alignment diagnosis -- firstdiv 126 -> 288 -> 290

## ROOTED 2026-06-23 (DEFINITIVE, with byte-exact raw-CP proof): pick-1953 is NOT overlap geometry -- it is the EQ-TREE INSERTION ORDER of the 3rd axiom's two faces, and the gated fix (use_wmo_insert_lr) DEMONSTRABLY reorders the rule-35 batch to WM's round-robin but EXPOSES an earlier latent cross-batch FIFO divergence at pick-113

The "CP-FORMATION OVERLAP-GEOMETRY" re-rooting immediately below (and the eq-tree-
DFS-interleave section under it) is REFUTED by a direct, orientation-aware match of
thvm's RAW (pre-normalization) critical pairs against WM's eight raw weight-109
collapse CPs (cp10320=-11, cp10321=-9, cp10323=-30, cp10324=-26, cp10325=-22,
cp10329=-29, cp10330=-25, cp10331=-21 in the banked /tmp/soa_align/wm.txt rule-35
batch). The match (tools/wm_align_sweep canon, no WM run) is unambiguous:

  WM partner  thvm j  thvm pos  thvm k1  thvm phase
  -11         20      [0]       1        0 (A/tops)
  -9          14      [0]       1        0
  -30         49      [0]       1        0
  -26         45      [0]       1        0
  -22         37      [0]       1        0
  -29         48      [0]       1        0
  -25         40      [0]       1        0
  -21         36      [0]       1        0

ALL EIGHT form at the SAME overlap position pos=[0] (preorder rank k1=1) in BOTH
engines -- there is NO geometry divergence (no deep-face x subterm-position spread,
no k1 fan-out). The 11-drill "WM spreads k1 / thvm collapses k1" root is FALSE: WM
also forms them all at the new rule's first-argument position. The independent WM
mirror (wm_order_sim2 over the banked trace) confirms pos (0,) for all eight, and
the U1_KPsBildenZuRegel source iterates TT(l) but every collapse partner unifies
at l|[0] (the d(x,y) arm) only.

### The real divergence: eq-tree DFS leaf-arrival ORDER of two leaf-groups
The eight split across THREE eq-tree leaves at the q=[3,-1,-2] (= d(x1,x2)) query
(THVM_WMO_ARRDUMP, nreg=71):
  arr12  leaf [3,3,-1,-1,-2]            chain -11,-9          (the {-11,-9} group)
  arr14  leaf [3,3,3,-1,3,-1,-1,-2,-3]  chain -29,-25,-21     (E29-distinguished face)
  arr16  leaf [3,3,-1,3,-2,3,-2,-2,-3]  chain -30,-26,-22     (E30-distinguished face)
thvm sorts by (phase,k1,k2,arr,ch): arr12 < arr14 < arr16 -> emits {-11,-9},
{-29,-25,-21}, {-30,-26,-22}. WM's SUE ages (7805,7806 | 7808,7809,7810 | 7814,
7815,7816) emit {-11,-9}, {-30,-26,-22}, {-29,-25,-21} -- the A,B,C round-robin.
So WM reaches the E30-group leaf BEFORE the E29-group leaf; thvm reaches them in
the opposite order. The {-11,-9} group is already in agreement; only the E29/E30
groups are swapped.

### Root cause: the 3rd axiom's two faces are inserted into the eq-tree in
### inverted order, which (via AltesBlattPolieren splice-after) flips E29/E30
The E29-group and E30-group leaves are AltesBlattPolieren parallels that splice
immediately AFTER the E2 (3rd axiom, thvm trace 13) face exits at the depth-1 d
node of the eq-tree: the E29 group follows the E2 face with sub d(d(x,x),y)
(cells [f,f,v,v,v], = E2's d(d(d(x2,x2),x1),...) rhs-first-arg side), the E30
group follows the E2 face with sub d(x,d(y,z)) (cells [f,v,f,v,v], = E2's
d(d(x1,d(x2,x3)),...) lhs-first-arg side). So the E29/E30 emission order is
determined entirely by which E2 face exit comes first at that node.

The validated mirror (wm_trie_mirror.py) keeps E2.face0 (sub d(x,d(y,z)), WM's
flat(l)) BEFORE E2.face1 (sub d(d(x,x),y), WM's flat(r)) throughout: face0 is
hung first (_hang head-insert :150/:163), face1 then splits face0's leaf and its
jump is spliced AFTER face0's (BlattAufgeteilt AltesBlattPolieren where='after',
:228-229). thvm INVERTED them (face1 before face0) at the very first insert: at the
trace-13 registration, atp_wmo_insert_fact_ex inserted the WM-DISTINGUISHED face
first, and for this CP-derived equation dist_rhs=1 (WM-distinguished = thvm stored
rhs = the d(d(x,x),y) face), so the rhs face entered the empty tree first and the
lhs face split-after -> face1 < face0. WM's tree INSERTION order instead follows
the STORED (LRSortieren) orientation flat(l) then flat(r) -- independent of the
distinguished face (KPLinks, a CP-formation property used only for the rank-key).

### The fix (gated, byte-identical OFF): use_wmo_insert_lr (THVM_ATP_WMO_INSERT_LR)
src/atp/wm_order.c atp_wmo_insert_fact_ex: when set, insert the equation's two
faces in WM's GleichungEinfuegen order -- the LRSortieren-Kleiner face first (via
atp_lr_sortieren_rec, key-side only, stored orientation untouched), the Groesser
face second. The chain WM-face LABEL still follows dist_rhs (lhs leaf = dist_rhs,
rhs leaf = !dist_rhs), so the wmo_tops_rank/wmo_leaflist_rank partner lookup is
byte-identical to the prior dist-first registration; ONLY the physical tree exit
order moves to WM's. VERIFIED to correct the rule-35 batch: with the gate ON the
E30-group leaf moves to DFS arrival 14 and the E29-group leaf to arrival 17 (was
14 / 16) -- the E30 group now precedes the E29 group, exactly WM's A,B,C round-robin
emission for picks 1953-1960. src/thvm.h use_wmo_insert_lr; setter
thvm_atp_set_use_wmo_insert_lr; bench env THVM_ATP_WMO_INSERT_LR.

### Outcome + the residual: ON exposes an earlier latent cross-batch FIFO at pick-113
GATES (OFF byte-identical, all met): FormationFifo firstdiv=1953 unchanged;
WALDMEISTER-only firstdiv=19 unchanged; bin/test_atp 136236/136236; 15 wm_pr
theorems CPSEL byte-identical vs clean HEAD c43a3f17 (BooleanAxioms And{Comm,Assoc},
DroppingAlwaysFalse, ImpliesRobbins_c1; HuntingtonAxioms DoubleNegation;
AbelianTarski Comm; McCune {InverseOfComposite, EqualityOfInverses};
HigmanNeumann RightIdentity; AbelianGroup ImpliesAbelianMcCune; AbelianMcCune
{Assoc, Comm, ImpliesMcCune, InverseOfComposite, InverseOfInverse}).
ON (THVM_ATP_WMO_INSERT_LR=1): firstdiv REGRESSES 1953 -> 113. The pick-113
regression is the documented exposure-cascade pattern (cf. WM_TRIE_FAITHFUL
standalone exposing pick-1884): the now-correct construction surfaces an EARLIER
latent divergence the wrong-but-masking dist-first order hid. At pick-113 WM and
thvm pick the SAME two weight-120 CPs (x.x # (y.(y.y)).x and x.(y.x) # (y.y).x)
in SWAPPED order -- a CROSS-BATCH FIFO-age inversion (the two CPs are formed in
DIFFERENT new-fact batches, so it is not a within-batch arrival sort like rule-35;
it is two batches' arrival ranks shifting relative to each other when the tree
moved). An LRSortieren-Kleiner-first variant of the insert order does NOT fix
pick-113 (still 113), and THVM_ATP_LR_SORTIEREN (reorient the STORED side, which
WOULD make thvm's stored orientation match WM's flat(l)) regresses harder to 22
because it perturbs CP formation (the KPAction order gate keys on cp.peak). So the
faithful insertion order is correct for the rule-35 batch but the global FIFO-age
reconciliation needs WM's per-equation stored orientation WITHOUT perturbing CP
formation -- the next lever. The fix is retained DEFAULT OFF as the proven
root-cause mechanism (rule-35 batch reordered to WM's round-robin, byte-identical
OFF) whose pick-113 exposure pins that next dependency.

Probes added (all env-gated, byte-identical OFF, pure getenv stderr):
THVM_WMO_TREEDUMP[_AT] = full eq/rule-tree node+exit structure dump for leaf/exit
diff vs the mirror (src/atp/wm_order.c wmo_maybe_treedump).

## RE-ROOTED 2026-06-23: pick-1953 is CP-FORMATION OVERLAP-GEOMETRY, not the eq-tree DFS arrival -- the "EQUATION-tree DFS interleave" attribution (section below) is REFUTED by the leaf layout; a round-robin re-key is the WRONG tool (regresses 1953 -> 52) [SUPERSEDED by the section above: there is NO geometry divergence; all 8 form at pos[0]/k1=1 in both engines]

The CORRECTED-2026-06-23 section below attributes pick-1953 to thvm's EQUATION-tree
DFS reaching the partner families in a GROUPED order where WM's combined scan
reaches them INTERLEAVED, and proposes extending the WM-trie-faithful construction
to the eq-tree to fix the arrival order.  Driven end-to-end against the banked
/tmp/soa_align/wm.txt (no WM run) with two new env-gated probes
(THVM_WMO_ARRDUMP = the eq-tree DFS leaf-arrival list with each leaf's chain;
THVM_WMO_RANKTRACE = the per-CP packed-key fields), this attribution is REFUTED:

### The eq-tree construction is ALREADY shared + faithful for BOTH trees
wmo_register -> wmo_tree_insert (src/atp/wm_order.c) is the SAME code for the rule
tree (tree=0) and the equation tree (tree=1), and it already passes w->polier_after
to both, so use_wm_trie_faithful's splice-after/jump-dedup/head-insert corrections
apply to the eq-tree too.  Verified: under THVM_ATP_WM_TRIE_FAITHFUL the rule-35
query's eq-tree arrivals are BYTE-IDENTICAL (C-leaf arr=5, B-leaf arr=6, A-leaf
arr=10, same chains) -- there is no separate, less-faithful eq-tree construction
branch to "extend".  wmo_tree_remove likewise carries the full collapse +
Sprung-reissue parallel-order restoration (the mirror's remove()).

### The 8 partners share THREE leaves, so a single DFS CANNOT round-robin them
For the f=52 batch (= WM rule 35; nreg=71) the eight weight-109 collapse CPs
`x1 # ((x1.<MID>).(x2.x1))` overlap rule-35 at ONE position (combo=1, poslen=1) on
the new rule's pos[1] subterm `x2.(x2.x1)`.  thvm's eq-tree DFS over that query
reaches the partner faces at exactly THREE leaves, each holding its whole
same-shape family chain (THVM_WMO_ARRDUMP, q=[3,-1,-2] x1.x2 at nreg=71):
  arr=5  leaf x1.(y.y)  chain = eq30(t6957,f1), eq29(t6535,f1), ...   = family C (2)
  arr=6  leaf x1.(y.x)  chain = eq11(t3993,f0), eq9(t3784,f0), ...,(t418,f1) = B (3)
  arr=10 leaf x1.(x.y)  chain = ...(t4752,f0),...,(t676,f1)           = family A (3)
thvm sorts the batch by (arr,ch) so each family's same-leaf chain emits together:
C,C,B,B,B,A,A,A (cp_seq 7739,7743,7749,7751,7752,7760,7762,7763, CPFORM trace).
WM's SUE ages 7805-7816 give A,B,C,A,B,C,A,B (round-robin: cp10320..cp10331
parents (35,-11/-9/-30/-26/-22/-29/-25/-21)).

The decisive structural fact: family C is the two equations eq29/eq30 whose
WM-INDEXED reverse faces are `x3.(x2.x2)` and `x3.(x1.x1)` -- BOTH canonicalize to
the IDENTICAL discrimination key `x.(y.y)`.  Identical keys MUST share a leaf in
ANY discrimination tree, WM's included (WM inserts both equation directions into
RE_Gleichungsbaum, RUndEVerwaltung.c GleichungEinfuegen:485-497, the reverse keyed
by TiefeRechts = its own left side).  So WM ALSO holds eq29/eq30's `x.(y.y)` faces
at one shared leaf, and WM's MitAllen (Unifikation1.c:1464-1468) processes a leaf's
whole chain in one pass.  Therefore WM's C ages 7808/7814 being NON-ADJACENT (a B
and an A age between them) is IMPOSSIBLE to produce from a single TT(l)=?E DFS over
that query -- no DFS-arrival ordering of thvm's (or WM's) leaves yields round-robin.

### Root: WM forms these 8 via DIFFERENT overlap geometry than thvm
The round-robin therefore is NOT an eq-tree DFS-arrival signal.  WM forms the 8 CPs
by matching each partner's DEEP DISTINGUISHED face against the appropriate subterm
position of rule-35 (U1_KPsBildenZuRegel section 2 iterates ALL of l's subterms,
:1527-1537), interleaving the families across positions; thvm forms the identical
8 CPs via the SHALLOW reverse `x.(y.y)`/`x.(y.x)`/`x.(x.y)` face at the single
pos[1] overlap, collapsing each family onto one leaf.  Same CP SET, DIFFERENT
(outer-position, inner-face) LINEAGE -> different emission order.  This is the
CP-formation OVERLAP-GEOMETRY class (atp_overlap_ij assigns a CP to a different
(outer, position) than WM), exactly what the firstdiv-19 / pick-1884 analyses and
the SUPERSEDED-2026-06-23 section flagged -- NOT the eq-tree construction.  The
prior "EQUATION-tree DFS interleave" framing mistook thvm's arr-grouped emission
for the cause; the cause is upstream, in which (face,position) thvm builds the CP
from.

### Why no re-key advances (measured)
A general WM-faithful round-robin re-key was implemented + measured (gated
THVM_ATP_EQTREE_RR, since reverted): re-key each A-phase tops (phase,k1,k2) group
column-major over its distinct arrival-leaf families.  It REGRESSES firstdiv
1953 -> 52 whether the families are ordered ascending or descending arrival and
whether or not restricted to equation partners -- because WM does NOT round-robin
in general (its emission IS the eq-tree DFS arrival = thvm's arr sort for the vast
majority of groups; the rule-35 appearance is the geometry artifact above, not a
universal interleave).  No leaf-arrival re-key can both produce the rule-35
round-robin and preserve the 1..1952 prefix.  Closing 1953 requires the
atp_overlap_ij geometry port (build these CPs from WM's deep-face/subterm-position
lineage so their k1/position spreads as WM's does), a deep cp-gen change whose
per-CP position assignment the no-WM-run constraint cannot validate (it needs WM's
overlap-position dump, not just the selection trace).  DEFERRED with this hard
structural proof, superseding the eq-tree-construction hypothesis.

GATES (this re-rooting is documentary + adds two env-gated diagnostics only):
bin/test_atp 136236/136236 (probes byte-identical OFF, pure getenv stderr);
FormationFifo soa firstdiv=1953 unchanged; WALDMEISTER-only firstdiv=19 unchanged;
12 wm_pr theorems CPSEL byte-identical vs clean HEAD c43a3f17 (BooleanAxioms
And{Commutativity,Associativity,Idempotence,Distributivity,Absorption} + DeMorgan_c1,
AbelianGroup {InverseOfComposite,InverseOfInverse,ImpliesMcCuneAxioms}, AbelianMcCune
{Commutativity,Associativity}, AbelianTarski Commutativity).  Probes:
src/atp/wm_order.c wmo_tops_rank (WMOARR, THVM_WMO_ARRDUMP[_LO/_HI]) + atp_wmo_rank
(WMORANK, THVM_WMO_RANKTRACE).

## CORRECTED 2026-06-23: pick-1953 is NOT over-formation -- slot48/49 ARE WM E#29/E#30; the residual is the EQUATION-tree DFS interleave (emission ORDER, not CP-SET)

The "thvm over-forms thvm-only `x.(y.y)=deep.x` equations j=48/49 WM never forms"
premise (the section immediately below, dated 2026-06-23, and the WM-trie sections)
is REFUTED by exact structural comparison against the banked /tmp/soa_align/wm.txt
(no WM run).  The prior claim rested on a flawed manual variable-comparison that
treated the trailing var as fresh.  With orientation-insensitive joint-variable
canonicalization:

  thvm slot48 `x.(y.y) = ((z.(z.z)).y).x`  ==  WM E#29 `((z.(z.z)).y).w = w.(y.y)`
    (rename w<->x: byte-identical equation; WM `...and added to E as such with
     number 29` at wm.txt L53986, parents ue (-21,-10))
  thvm slot49 `x.(y.y) = (y.(z.(z.z))).x`  ==  WM E#30
    (WM `added to E ... number 30`)

WM keeps 29 distinct E-members and BOTH slot48/49 are among them.  So thvm forms the
SAME CP set as WM at the pick-1953 batch -- there is NO over-formation to suppress.
KPActionGR (the outer order test on the slot48 source overlap, outer rule36 eq
`x.(y.x)=((z.(z.z)).y).x` x inner rule19 `x.(x.(x.y))->y.x`, combo-0 ROOT) is
KBO-INCOMPARABLE in thvm (cp.rhs `((z.(z.z)).y).x` vs peak `x.(y.x)`: var x appears
twice in peak, once in rhs -> KBO_UN) so thvm KEEPS it -- and WM's
`TermGroesserUnif(KPLinks,Ueberlappung)` returns the same KBO_UN verdict, so WM
keeps it too.  thvm's atp_cp_order_gate IS WM's KPActionGR/GG byte-for-byte here.

### THE REAL pick-1953 residual: equation discrimination-tree DFS arrival order
At the f=52 batch (= WM rule 35) the eight weight-109 collapse CPs `((x.<mid>).(y.x))
= x` are IDENTICAL in both engines (same 8 partner equations, same A/B/C `<mid>`
shapes); only the EMISSION ORDER differs:
  WM   : A,B,C,A,B,C,A,B  (round-robin -- picks 1953=(35,-11) 1954=(35,-9)
                           1955=(35,-30) 1956=(35,-26) 1957=(35,-22) 1958=(35,-29)
                           1959=(35,-25) 1960=(35,-21))
  thvm : C,C,B,B,B,A,A,A  (grouped by shape)
All 8 thvm CPs carry phase=0 (tops/A), k1=1 (SAME overlap position [1]), k2=1
(eq-tree); they differ ONLY in k3=arr (the eq-tree DFS leaf-arrival rank computed by
wmo_tops_rank/wmo_dfs): C-family arr=5, B-family arr=6, A-family arr=10.  thvm sorts
arr5<arr6<arr10 -> grouped.  WM's single combined eq-tree scan (Unifikation1.c
U1_KPsBildenZuGleichung phase A `TT(l) =? E`, one TermMitDSBaumUnifizieren walk per
Index over MitAllen at each leaf) reaches the families INTERLEAVED.  The raw DFS
arrival list (THVM_WMO_ARRDUMP probe, qn=3 face=1) is C(arr5),B(arr6),A(arr10) --
thvm's eq-tree GROUPS the same-shape leaves where WM's tree interleaves them.

So the divergence is the EQUATION discrimination-tree CONSTRUCTION (the order
BlattAufgeteilt chains/parallels the leaves), NOT the walk (the recursive DFS is
faithful for a fixed tree, proven by the mirror) and NOT cp-gen geometry (the CP set
matches).  THVM_ATP_WM_TRIE_FAITHFUL leaves this arrival order BYTE-IDENTICAL
(verified: arr 5/6/10 unchanged ON) because it fixes only the RULE tree; the 1953
partners (jtr 3784/3993/6535/6957) are eq-tree splice-after parallels at
i-start_pos>1, untouched by the rule-tree head-insert fix.

### NO scoped re-key advances 1953 without regressing the 1..1952 prefix
- The co-rank logic (wmo_eq_sides_var_differ -> anchor both faces on the earlier
  leaf) is load-bearing for the prefix: disabling it (THVM_X_NO_CORANK probe)
  regresses firstdiv 1953 -> 53.  It is correct for the asymmetric oriented-variant
  equations it targets; it is just not the lever for the E#29/30-family round-robin.
- Swapping k3<->k4 (chain-major) yields C,C,B,A,B,B,A,A -- not WM's A,B,C round-robin.
- WM's partner order within a family is NON-monotonic in birth index (A = -11,-26,-25
  i.e. 11,26,25), confirming it is the eq-tree chain/arrival order (head/tail
  BlattAufgeteilt insertion), reproducible ONLY by porting WM's full equation
  discrimination-tree construction -- the multi-session effort the rule-tree TRIE
  work demonstrates, and the one the no-WM-run constraint cannot validate beyond
  firstdiv.  DEFERRED to the eq-tree construction port (the genuine next lever).

## SUPERSEDED 2026-06-23 (premise refuted above): pick-1953 is NOT intake-orientation (LRSortieren/dist_rhs) -- it is a CP-formation overlap-geometry divergence (thvm forms a CP WM never forms, keeping an `x.(y.y)=deep.x` equation WM lacks)

The pick-1953 residual was driven end-to-end with the banked WM ref
(/tmp/soa_align/wm.txt, no WM run) to settle the orientation hypothesis. It is
NOT an intake-orientation issue: WM does NOT reorient derived equations
(LRSortieren runs only at SPECIFICATION intake, SpezNormierung.c:758-791
SN_SpezifikationAuswerten -> LRSortieren over get_equations(); a DERIVED equation
keeps its CP lhs unchanged in RE_ErzeugteGleichung, RUndEVerwaltung.c:411-429,
where the distinguished face RichtungAusgezeichnet=TRUE rides selRec.lhs). WM's
distinguished face is KPLinks = MGU(TP_RechteSeite(Vater)) = sigma(r of the OUTER
rule), Unifikation1.c:916 -- a CP-FORMATION-GEOMETRY property, NOT recoverable
from stored content (E#29/30 distinguish the DEEP left side; E#21-28 the SHALLOW
left side; the same equation feeds multiple batches).

### THE EXACT pick-1953 batch (instrumented, runtime BATCH/CPFORM traces)
All seven diverging w=10 collapse-CPs `((x.m).(y.x)) = x` are formed in ONE outer
rule's batch (thvm f=52, i=52), overlapping seven partner equations at combo=1:
  C-form (m=y.y): partners j=49,48 (cp_seq 7739,7743)
  B-form (m=y.x): partners j=37,36,14 (cp_seq 7749,7751,7752)
  A-form (m=x.y): partners j=45,40 (cp_seq 7760,7762)
WM emits A,B,C round-robin (picks 1953,1954,1955,1956,1957...) from its single
combined superposition scan; thvm forms C,C,B,B,B,A,A because the partners' tops-
DFS arrival ranks (arr, keyed by j_face_wm = j_face XOR dist_rhs) order them
C<B<A. The partners j=37/36/45/40 map exactly to WM E#22/E#21/E#26/E#25
(distinguished = the shallow `x.(...)` side, thvm dist_rhs=0, CORRECT); j=14 maps
to E#11 (distinguished = the larger `(x.x).y` side, dist_rhs=1, CORRECT).

### WHY ORIENTATION CANNOT FIX IT (dist_rhs lever tested, regresses)
The C-form partners j=48/49 are equations `x.(y.y) = (y.(z.(z.z))).x` and
`x.(y.y) = ((z.(z.z)).y).x`. WM's E-set NEVER contains an `x.(y.y) = deep.x`
equation (it keeps `x.(y.y)` only in E#10 `x.(y.x)=x.(y.y)`, E#12 `x.(y.y)=x.(x.y)`,
rule 12, rule 46) and WM NEVER forms the originating CP (`d(x1,d(x2,x2)) # d(d(x2,
d(x3,d(x3,x3))),x1)` is absent from the full -a4 trace). So j=48/49 are thvm-only
equations born from an overlap WM does not perform -- a CP-formation geometry /
under-reduction difference: WM reduces the `x.(y.y)` face (via E#12-class ordered
rewriting) so the equation collapses to an E#26-shape, while thvm keeps it.
A precisely-scoped dist_rhs override (THVM_ATP_NARROW_ORIENT, a shallow-`v.(p.q)`-
side pin gated on the OTHER side carrying a side-only self-cube `c.(c.c)` -- it
fired on EXACTLY j=48 and j=49, verified by trace) REGRESSES firstdiv 1953 -> 1581:
those same two equations' emission face also feeds the earlier pick-1581 w=120
batch (E#12 `x.(y.y) # x.(x.y)`), where the heuristic dist_rhs=1 aligns with WM
but the "faithful" dist_rhs=0 does not. No single dist_rhs value reconciles both
batches -> it is geometry, not orientation. The knob was therefore NOT landed
(it only regresses).

### NEXT LEVER (the real one): ordered-rewrite the `x.(y.y)` face / CP-formation geometry
Closing 1953 needs thvm to either (a) NOT form the j=48/49 overlap (WM's exact
TT(l)=?E vs l=?eTT outer/inner geometry, Unifikation1.c U1_KPsBildenZuRegel:
1525-1547, so the CP is assigned WM's outer/position and is never built here), or
(b) ordered-reduce the `x.(y.y)` face of those equations to `x.(x.y)` via the
E#12-analog the way WM's unfailing-completion rewrite does, collapsing j=48/49 to
the E#26-shape WM keeps. Both are downstream of the CP-formation/normalization
subsystems, NOT intake orientation. This is the SAME overlap-geometry frontier the
1884 round characterized (atp_overlap_ij assigns a CP to a different (outer,
position) than WM).

## RESOLVED 2026-06-22: pick-1884 CP-formation geometry -- the immediate-strict-ancestor head jump is WM-faithful in BOTH construction modes (1884 regression closed; TRIE foundation now non-regressing)

The prior round (below) showed THVM_ATP_WM_TRIE_FAITHFUL standalone REGRESSES 1953 ->
1884 by a CP-formation overlap-geometry divergence at the rule-44 partner batch.
That divergence is now ROOT-CAUSED and FIXED.

### ROOT CAUSE (instrumented, not a hunch)
The 1884/1885 pair is two phase-A tops CPs with the SAME outer rule (thvm f=56 = WM
rule 44), SAME combo (0), SAME overlap position [0] -- they differ ONLY in the
partner rule and that partner's tops-DFS arrival rank (k3):
  A = WM CP 12261 parents (44,39), w2=9061: partner rule 39 = dot(dot(x,x),dot(x,y))->x
  B = WM CP 12262 parents (44,37), w2=9062: partner rule 37 = dot(dot(x,x),dot(y,x))->x
WM forms A (partner 39) before B (partner 37); thvm's raw atp_wmo_rank keyed them
swapped (B's keyraw < A's, differing by exactly 1 in k3 = one 2^28 unit).  The two
partner leaves share the prefix dot(dot(v1,v1),dot(...)) and split at the inner-left
position: A stores the REPEATED tree var (v1), B a FRESH tree var (v2).  The query
(rule 56's [0] subterm = dot(v1,v2)) reaches both via an ancestor parallel jump
built by AltesBlattPolieren.

The bug was NOT the tops DFS walk and NOT the arrival ranking -- both are faithful.
It was the CONSTRUCTION: at the depth-4 start node, the A-leaf's parallel is a
NeuesBlattEinhaengen immediate-strict-ancestor jump (start_pos=4, i=5, j=6,
i-start_pos==1, e_new=7 < e_old=11) that WM HEAD-inserts (RumpfSprungeintragSetzen
:466-467, consulted first), so A's leaf arrives BEFORE B's.  thvm head-inserted it
under the historical path (POLIER-PAR-HEAD) but the TRIE-faithful path
(use_wm_trie_faithful) had gated the head-insert behind `!polier_after`, forcing
splice-AFTER (POLIER-PAR-AFTER) -- moving A's parallel behind B's, swapping the CPNr
ages.  WMOCT confirmed: OFF=POLIER-PAR-HEAD (A first), ON=POLIER-PAR-AFTER (A last).

### THE FIX (src/atp/wm_order.c wmo_altes_blatt_polieren :856-869)
The immediate-strict-ancestor head-insert (start_pos < i && e_new < e_old &&
i-start_pos==1) is WM-faithful in BOTH construction modes -- it reproduces the
NeuesBlattEinhaengen head jump regardless of the AltesBlattPolieren splice
convention.  Dropped the `!polier_after` guard so the head-insert applies whether or
not use_wm_trie_faithful is set; the splice-after convention now governs only the
non-immediate-ancestor parallels (i-start_pos > 1, the soa rule-35 @1953 batch +
MeredithAxioms And/OrAssoc @340).  OFF is byte-identical by construction (the OFF
predicate `!polier_after && ...` already reduced to the same condition).

### RESULT
FormationFifo + THVM_ATP_WM_TRIE_FAITHFUL: firstdiv 1884 -> 1953 (the 1884 pair
emits A-before-B at WM's CPNr ages, restoring parity through 1952).  Gates: OFF
byte-identical (WALDMEISTER-only soa firstdiv=19, FormationFifo soa firstdiv=1953,
1..126734 / 1..121177 CPSEL prefixes byte-identical vs clean HEAD 2b24dfe7 -- the
only diffs are wall-cutoff tail appends); bin/test_atp 136236/136236; 7-theorem
wm_pr corpus byte-identical OFF (Sheffer Commutativity, Boolean OrAssoc 1266,
AbelianGroup InverseOfComposite, McCune Associativity 298, Wolfram Commutativity
3219, Boolean DoubleNegation, Group InverseOfInverse).

### REMAINING FRONTIER at pick-1953 (DIFFERENT class: EQUATION-tree round-robin)
With the 1884 regression closed, FormationFifo+TRIE+fix lands AT 1953 -- the SAME
firstdiv as plain FormationFifo, because the TRIE rule-tree construction has ZERO
effect on the 1953 batch (the keyraw is byte-identical FFifo vs FFifo+TRIE+fix
there).  The 1953 residual is an EQUATION-tree (tree=1) ordering, not the rule-tree
geometry the 1884 fix addresses.  Outer thvm f=52 = WM rule 33; the surviving CPs
are combo=1 reverse-face overlaps onto the equation tree, normalized to weight 109
`x1 # dot(dot(x1,dot(?,?)),dot(x2,x1))`.  WM emits these ROUND-ROBIN over partner
equations (CP 9856 parents (33,-11)=A `dot(x1,x2)`, 9857 (33,-9)=B `dot(x2,x1)`,
(33,-2)=C `dot(x2,x2)`, then repeat: A,B,C,A,B,C...), but thvm GROUPS them by shape
(C,C,B,B,B,A,A,A) -- it sorts by per-partner eq-tree arrival (k3) + co-rank chain
(k4), so each partner's faces emit together.  The eq-tree partners (jtr 3784/3993
/6535/6957) are built via PREPEND + POLIER-PAR-AFTER at i-start_pos>1 (genuine
splice-after parallels, NOT the immediate-ancestor head case), so the 1884 fix does
not reach them.  Closing 1953 needs WM's single COMBINED eq-tree scan emission order
(one face per partner per scan position, cycling partners) rather than thvm's
per-partner enumeration + arrival/co-rank sort -- a deeper eqn-tree CP-formation
restructure, DEFERRED (high cross-theorem regression risk; no-WM-run constraint
forbids the validation sweep a DFS-order change would need).

## INVESTIGATED 2026-06-22: THVM_ATP_WM_TRIE_FAITHFUL -- AltesBlattPolieren splice-after construction (firstdiv-1953 root-caused; net-advance blocked by a CP-formation geometry frontier)

The pick-1953 residual (the rule-35 / equation-{-11,-9,-30,-26,-22,-29,-25,-21}
emission batch where WM emits the partner shapes A/B/C round-robin and thvm groups
them) was traced END-TO-END against the validated WM trie mirror
(tools/baselines/wm_order_sim/wm_trie_mirror.py + wm_order_sim2.py).

### ROOT CAUSE (mirror-validated, NOT a hunch)
The rule-35 tops batch overlaps the new rule's subterm `dot(v1,v2)` (position [0])
against the equation tree.  Dumping thvm's runtime tops-DFS leaf-arrival order for
that exact query vs the mirror's faithful tree, the leaf SET and chain groupings
are IDENTICAL except at arrivals 6-8: thvm visits the B-leaf `dot(v1,dot(v2,v1))`
at arrival 6, but WM (mirror) visits it at arrival 8 -- AFTER the two deeper
function/jump leaves `dot(v1,dot(v2,dot(v2,v2)))` and `dot(v1,dot(v2,dot(v3,..)))`.
That single 6<->8 leaf-order inversion is what scatters the round-robin batch into
thvm's grouped order.  The inversion comes from CONSTRUCTION, not the walk: WM's
AltesBlattPolieren (DSBaumOperationen.c :523-526) ALWAYS splices a BlattAufgeteilt
parallel jump immediately AFTER its model survivor ("hinter den Eintrag setzen"),
but thvm head-inserted it in one special case (the historical POLIER-PAR-HEAD),
moving the split leaf's parallel face to the wrong outgoing-list (= DFS-arrival)
position.

### THE WALK IS NOT THE LEVER (recursive == cursor for a fixed tree)
The mirror reproduces WM's emission order through rule 39 using a RECURSIVE tops
DFS (wm_order_sim2.py tops_arrivals) -- byte-identically to WM's single-cursor
backtracking walk (Unifikation1.c RumpfTermMitDSBaumUnifizieren + Delta*).  For a
FIXED tree the two walks visit leaves in the same order (same per-node child
ordering: exact-symbol child, then var children ascending symbol-code, then jump
exits; WM's AbstiegVerwalten continuation chaining is equivalent to recursion).
So the divergence is the TREE STRUCTURE the walk runs over, i.e. the construction.

### THE CONSTRUCTION FIX (src/atp/wm_order.c, gated use_wm_trie_faithful)
Three corrections, all gated behind THVM_ATP_WM_TRIE_FAITHFUL (DEFAULT OFF,
byte-identical OFF):
  1. AltesBlattPolieren parallels splice AFTER the survivor (never head-insert);
     the POLIER-PAR-HEAD special case is kept only when the flag is off.
  2. Jump dedup (wmo_jump_find): a (sub cells, target) jump already at a start
     node is never re-created -- the mirror's _add_entry :88-90.
  3. PositionNachTeilterm uses the surviving jump's OWN stored sub_len
     (start_pos + surv->sub_len), the mirror's `land = start_pos + len(ent.sub)`
     :204/:230, not a re-walk of the old key from start_pos.
VALIDATED: with the gate ON the runtime tops-DFS leaf-arrival order at the rule-35
query now reads arr6=`dot(v1,dot(v2,dot(v2,v2)))`, arr7=`dot(v1,dot(v2,dot(v3,..)))`,
arr8=B-leaf -- EXACTLY the mirror's (= WM's) order.  The construction fix is
faithful and correct for the 1953 divergence.

### WHY IT DOES NOT NET-ADVANCE (the next frontier, characterized precisely)
Applied alone the construction fix REGRESSES firstdiv 1953 -> 1884, exactly as the
prior round predicted: it exposes a separate divergence at pick-1884 the
head-insert had masked.  Picks 1884/1885 are a 2-element transposition of the CP
faces `dot(v,dot(dot(v,w),v)) # dot(v,w)` (A, WM wants first) and
`dot(v,dot(dot(w,v),v)) # dot(w,v)` (B).  WM forms BOTH in rule-44's batch:
pick-1884 = parents (44, 39) [partner rule 39 = A], pick-1885 = parents (44, 37)
[partner rule 37 = B], A before B.  thvm forms them with DIFFERENT outer rules --
A from slot 100 (partner slot 80), B from slot 98 (partner slot 77) -- so slot
98's batch (B) ages BEFORE slot 100's (A), giving B before A.  This is the SAME
CP-formation OVERLAP-GEOMETRY class as the firstdiv-19 / firstdiv-1953 analyses:
thvm's atp_overlap_ij assigns a critical pair to a DIFFERENT (outer-rule,
position) than WM does, scattering its CPNr age across batches.  It is NOT the
discrimination-tree walk (proven faithful via the mirror) and NOT the construction
(now WM-faithful); it is the overlap-direction/position assignment in cp-gen, a
deeper frontier that needs porting WM's exact TT(l)=?E vs l=?eTT outer/inner
geometry (Unifikation1.c U1_KPsBildenZuRegel:1525-1547) so a CP inherits WM's
position rank.  The mirror itself stops reproducing WM at rule 40 (a single
out-of-order CP with a RULE-37 partner), confirming a construction/geometry detail
beyond what the current mirror models begins there.

### GATES (all hold; the construction fix is a gated, byte-identical-OFF foundation)
- DEFAULT OFF byte-identical: bin/test_atp 136236/136236 (+ a new
  atp/wm-trie-faithful-altes-blatt-polieren-splice-after unit test); WALDMEISTER-
  only soa firstdiv=19 unchanged; FormationFifo soa firstdiv=1953 unchanged (the
  flag is NOT auto-enabled under FormationFifo -- enabling it regresses to 1884);
  wm_pr corpus spot-check (Sheffer Commutativity, Boolean OrAssoc 1266, AbelianGroup
  InverseOfComposite, McCune Associativity 298, Wolfram Commutativity common-prefix
  2405) CPSEL byte-identical vs clean HEAD 0a0dcfd5.
- THVM_ATP_WM_TRIE_FAITHFUL ON: validated to reproduce WM's tops-DFS leaf-arrival
  order for the rule-35 batch; standalone regresses 1953->1884 (the exposed
  geometry frontier above), so it ships OFF, as the foundation for the cp-gen
  geometry port that closes 1884+.

## ADVANCED 2026-06-22: THVM_ATP_DRAIN_REVFACE -- GMInterred reducible-face drain order

Atop FormationFifo+DRAIN_CHAINPOS (firstdiv=1558), the residual at pick-1558 was
the cube-mirror pair (thvm seq 3706/3707, WM `ue (-14,0)`/`ue (-18,0)`): WM picks
`-14` first, thvm picked `-18` first.  The 1505-era diagnosis framed this as a
within-pass chain-order transposition that DRAIN_CHAINPOS could not capture; the
real root is the DRAIN PULL FACE.

ROOT (WM file:line): WM's IR REPuffer is NOT filled by the distinguished-face
leaf list.  GMInterred (Interreduktion.c:285-307) runs RE_forGMReferenzen =
BK_ReferenzDurchlauf (DSBaumKnoten.h:499-514), which walks the Gleichungsbaum
leaf list and tests NF_ObjektAnwendbar(Objekt, .) per leaf -- it pulls each
equation through the face the new object REDUCES, not its distinguished
(LRSortieren) face.  WM indexes BOTH twins of an unoriented equation, so the two
faces live in DIFFERENT leaves; the victim re-enters the FIFO (and gets its late
w2 = ++CPNr) at its REDUCIBLE face's leaf-list position.

THE PROOF (instrumented thvm wmo tree at pick-711, the pass that demotes both):
the distinguished (`x.x`) face collides BOTH into equation leaf rank 1 (chain
[3089/-18, 2092/-14, 221, 184], newest-first), so face-0 ranking ties them and
DRAIN_CHAINPOS falls back to head-first = 3089/-18 first (wrong).  Their REVERSE
(cube) faces sit in DISTINCT leaves: 2092/-14 at reverse-leaf rank 14, 3089/-18
at reverse-leaf rank 15 -- so GMInterred drains 2092/-14 BEFORE 3089/-18, exactly
WM's order.  The same mechanism explains the 1505 pair (2817/-17 reverse-leaf 15,
2570/-16 reverse-leaf 16 -> -17 before -16) which head-first only matched by
coincidence (both their face-0 entries sat in leaf 1 at ascending chainpos).

THE FIX (src/atp/wm_order.c atp_wmo_victim_drain_key + src/atp/_.c demote sites):
the drain key now ranks a victim by the leaf-list position of the face the new
rule actually reduced -- reduced_thvm_side ^ dist_rhs, the same face remap
atp_wmo_eq_*_rank uses (wm_order.c:1834).  The three demote sites pass the
reduced side (LHS-collapse / bwd_demod = side 0; unorientable RHS reduce = side
1).  Mono / single-face victims fall back to face 0.  Gated behind
use_drain_revface (THVM_ATP_DRAIN_REVFACE; DEFAULT OFF; also turned ON under
FormationFifo).

RESULT: firstdiv 1558 -> 1953 (advance 395).  ON-vs-OFF prefix 1..1952
        weights-exact; OFF byte-identical by construction (the gated branch keeps
        the prior face-0 key).  Verified vs clean HEAD (1938c08e): WALDMEISTER-
        only / base-4knob / FFifo-without-revface all CPSEL BYTE-IDENTICAL (full
        41591/41597-line traces); WALDMEISTER-only firstdiv=19 unchanged;
        bin/test_atp 136234/136234 (+ a new revface drain unit test); wm_pr
        corpus spot-check (Sheffer Commutativity, Boolean OrAssoc 1266,
        InverseOfInverse, McCune Associativity 298) CPSEL byte-identical.

RESIDUAL at pick-1953 (different class, NOT the IR-drain schedule): WM's pick is
`ue (35, -11)` -- a RULE-OVERLAP CP (rule 35 superposed on equation -11), part of
a large rule-35 emission batch.  This is the global CP-formation emission order
for rule overlaps (atp_wmo_rank), the same class soa's firstdiv-19 analysis
flagged, not the interreduce pass schedule.  DEFERRED.

## ADVANCED 2026-06-22: THVM_ATP_DRAIN_CHAINPOS -- re-derivation drain within-leaf chain tiebreak

Atop FormationFifo (firstdiv=1505), the residual at pick-1505 is a CLEAN ADJACENT
2-ELEMENT TRANSPOSITION of the w=224 nested cube-mirror pair, re-stamped by the
re-derivation drain atp_wm_demote_drain (NOT atp_wmo_rank).  The two victims
(thvm seq 3506/3507, WM `ue (-17,0)`/`ue (-16,0)`) are demoted in ONE interreduce
pass and share a discrimination-tree leaf (distinguished face = the `x.x` side,
wmo equation tree leaf rank 1).  atp_wmo_victim_drain_key keyed victims ONLY by
leaf-list rank, so the two collided on one key and the stable sort fell back to
thvm's slot-scan push order -- the OPPOSITE of WM's chain order.

ROOT (WM file:line): WM drains the IR REPuffer that GMInterred / RMLinksInterred
filled (Interreduktion.c:288-339) via RE_forGleichungenRobust / RE_forRegelnRobust
= BK_forRegelnRobust (DSBaumKnoten.h:482-495): leaves in BK_NachfBlatt order, and
WITHIN a leaf the fact chain HEAD-FIRST following TP_Nachf.  The chain PREPENDS on
insert (RegelHinzufuegen, DSBaumOperationen.c:343-346), so head-first = chain
index 0,1,2.. ascending.  THVM_ATP_DRAIN_CHAINPOS folds the victim's within-leaf
chain index into the drain key as the low-order tiebreak below leaf-list rank
(src/atp/wm_order.c atp_wmo_victim_drain_key), so same-leaf victims drain
head-first exactly like BK_Regeln -> Nachf.  Gated behind use_drain_chainpos
(DEFAULT OFF; also turned ON under FormationFifo).

RESULT: firstdiv 1505 -> 1558.  ON-vs-OFF prefix 1..1504 byte-identical, prefix
        1..1557 weights-exact; WALDMEISTER-only common prefix byte-identical
        (clean HEAD md5 073e9247...); bin/test_atp 136232/136232; wm_pr corpus
        spot-check (DoubleNegation/InverseOfInverse/BooleanOrAssoc/Sheffer
        Commutativity/McCune InverseOfComposite/Wolfram Commutativity) CPSEL
        common-prefix byte-identical vs clean HEAD.  KNOB OFF (default):
        byte-identical by construction (the gated branch returns the prior key).

RESIDUAL at pick-1558 (re-derivation SCHEDULE, deeper frontier -- NOT a shape
knob): the next cube-mirror pair (thvm seq 3706/3707, WM `ue (-14,0)`/`ue (-18,0)`)
is ALSO a same-leaf pair, but WM emits them in OPPOSITE-of-chain-order (`-14`
older, head=`-18` newer; WM picks `-14` first).  thvm demotes both in ONE pass
(seq 3706 then 3707 by head-first chain); WM's `-14` carries a smaller w2 because
WM reduces it in an EARLIER interreduce pass.  Confirmed lineage-dependent, NOT
shape: the depth-1 cube-mirror pair (picks 290/291, `ue (24,-7)`/`ue (24,-6)`,
genuine TWO-parent superposition) emits RIGHT-before-LEFT -- the OPPOSITE shape
order to the re-derivation cases at 1505/1558 -- so a LEFT-before-RIGHT shape rule
would mis-order it.  Matching pick-1558 requires reproducing WM's interreduce pass
SCHEDULE (which new rule reduces which equation in which pass), which thvm's
slot-by-slot interreduce does not; the wmo chain order itself is faithful (thvm
chain [3089,2817,2570,2092] == WM eqn order [-18,-17,-16,-14]).  DEFERRED.

## RESOLVED 2026-06-22: THVM_ATP_CUBE_ARRIVAL -- w=224 double-cube k3-arrival swap

Atop the 8 prior knobs (firstdiv=1320), the residual at pick-1320 is a CLEAN
ADJACENT 2-ELEMENT TRANSPOSITION in rule19's (thvm slot28's) weight-224 birth
batch.  Both CPs are formed by both engines, both selected, adjacent; #1322+
realigns.  The swap (orientation-independent canon, banked /tmp/soa_align/wm.txt
selections 1320/1321 = `ue (19,-7)` / `ue (19,-2)`):
  - WM #1320 = B = `(x.(x.x)).y = (z.(z.z)).y`   (the DOUBLE-CUBE; rule19 x eqn7
                                                  = thvm slot28 x slot8 combo=0)
  - WM #1321 = A = `(x.(y.x)).z = ((y.y).x).z`    (the slot15-wrapped CP; rule19 x
                                                  eqn2 = thvm slot28 x slot2 c1)
  - thvm emits A (seq1126) BEFORE B (seq1127); WM emits B before A.

In atp_wmo_rank's packed key A and B share the ENTIRE A-phase tops group prefix
(phase=0, k1=3, k2=1, k4=0, k5=0) and differ ONLY in k3 = the partner equation's
discrimination-tree arrival rank: thvm gives the eqn2 partner arr=8 (earlier)
than the cube eqn7 partner arr=10, so A sorts first; but WM's single
superposition scan over rule19's overlap position surfaces eqn7 first.  This is
the SAME k3-arrival emission-order class as REVFACE_GROUP/POSGROUP, one weight
band up (w=224 vs w=120), with two DIFFERENT-shape oriented-equation partners
rather than the reverse-face / cube-reuse cases those handle.

THVM_ATP_CUBE_ARRIVAL detects the double-cube CP (atp_pair_is_double_cube,
reusing atp_term_is_cube_dot_var on both sides: same trailing y, distinct cube
vars) in the A-phase tops group and re-keys it to one BELOW its immediate
same-group predecessor -- the largest-keyed strictly-earlier same-group CP whose
normalized shape is the slot15-wrapped form (atp_pair_is_slot15_wrapped: the
slot15-term `x.(y.x)=(y.y).x` with a trailing `. z` on both sides) -- swapping
the adjacent pair into WM's order.  Scoped HARD: both shapes exact
(orientation-insensitive), A phase, identical group prefix (phase|k1|k2), anchor
strictly earlier-keyed -- never a generic equal-weight reorder.

RESULT: firstdiv 1320 -> 1505, wm-only=0, prefix 1..1504 byte-identical,
        weights-exact.  ON-vs-OFF prefix 1..1319 md5-identical
        (8f3dc87dba4cfe05b18246a6705665ab).
KNOB OFF (default): byte-identical -- bin/test_atp 136232/136232 (the separate
        test_atp_ac_abelian_repro stays at the documented pre-existing 1/3);
        8-knob soa firstdiv still 1320; WALDMEISTER-only firstdiv still 19
        (CUBE_ARRIVAL never fires without the other knobs' context).  atp.wlt
        63/2 identical ON vs clean-HEAD (the 2 are pre-existing).
NEXT (firstdiv=1505): ANOTHER clean adjacent 2-swap in the SAME w=224 band, one
        overlap-position class up -- a cube-nested sibling pair (WM #1505 = `x.x
        = x.(y.((z.(z.z)).y))` vs its rotation, thvm 1505<->WM 1506).  Same
        k3-arrival emission-order class; tractable by a sibling scoped knob.
        The recurrence across the w=224 band (1320 + 1505) reinforces the
        soa.txt note that a UNIFIED raw-arrival + bracketing port would
        eventually subsume the per-band knobs.

## RESOLVED 2026-06-21: THVM_ATP_POSGROUP -- A-phase raw-arrival cube grouping

Atop the 6 prior knobs (firstdiv=966), the residual is a w=120 A-phase batch
(soa f=41, rules=42).  All 7 emitted CPs share phase=0/k1=8/k2=1; they differ in
k3 (tops DFS partner-leaf arrival).  WM emits them in RAW arrival order with one
exception; thvm diverges in two ways introduced by REVFACE_GROUP at this band:

  (a) REVFACE pulls the `(x.(x.x)).y = y.y` cube CP of a vd=0 PERMUTATION
      partner (j33, raw arrival 7) up beside the SAME-shape CP of a vd=1
      ORIENTED partner (j7, arrival 1), jumping 16 intervening same-group CPs.
      WM keeps j33 at its own raw arrival (j7 and j33 are independent leaves
      surfaced at their own ages, not one re-derived equation).  POSGROUP
      un-groups exactly this firing: the cube shape with a vd=1 oriented anchor.

  (b) The `(x.x).y = (x.y).y` permutation partner's REVERSE face (j19 face0,
      raw arrival 6) precedes the two arrival-7 cube CPs; WM brackets the
      permutation partner's two faces around them (forward at 967, reverse at
      970).  POSGROUP defers the reverse face past the largest same-group cube
      arrival.

Both parts are scoped HARD by NORMALIZED CP shape (atp_pair_is_posgroup_cube =
`(x.(x.x)).y = y.y`; atp_pair_is_seq564_sibling = `(x.x).y = (x.y).y`), the A
phase, identical group prefix, and the vd=0-permutation / vd=1-oriented split --
never a generic equal-weight reorder.  A new key_raw field on AtpWmoCpEnt
captures atp_wmo_rank's result before the re-key passes so the deferral reads
true tree arrivals.

RESULT: firstdiv 966 -> 1320 (clears the whole w=120 cube band), wm-only=0,
        prefix 1..1319 byte-identical, weights-exact.
KNOB OFF (default): byte-identical -- bin/test_atp 136232/136232; 6-knob soa
        firstdiv still 966; ZeroIsAbsorbing@5000 trajectory identical ON and OFF
        (step1500 rules=370 cps=315840, final steps=2404 rules=508 cps=579859).
NEXT (firstdiv=1320): a w=224 double-cube batch (rules=48) -- `(x.(x.x)).y =
        (z.(z.z)).y` vs `((x.x).y).z`, the SAME cube/permutation family one
        weight band up.  The recurrence across weight bands (w=120, w=224, and
        likely w=155/w=209) argues a UNIFIED U1 raw-arrival + reverse-face
        bracketing port now subsumes the per-band knobs better than continuing.



## RESOLVED 2026-06-21: THVM_ATP_COMM_DROP_DUP re-ages the one early slot15-term copy
Atop COMM_REAGE (which lands firstdiv at 288), the residual at pick-288 is the
DUPLICATE re-derivation of slot15's OWN term `x.(y.x) = (y.y).x` (canon
`(x.x).y = y.(x.y)`).  slot15 (the rule of this exact shape) has been LIVE since
pick-54, so this copy is redundant: thvm forms it in rule34's birth batch and keys
it ONE FIFO slot too early (cp_seq 1523), so it pops at pick-288.  WM does NOT drop
it -- WM RE-SELECTS a slot15-class copy one slot LATER, at pick-289 (its in-batch
`x.(x.x) = y.(y.y)` successor, cp_seq 1524, is WM's pick-288).  Both engines select
the slot15-class term 15 times at the IDENTICAL positions 54,101,107,110,113,121,
140,149,205,208,267,275,_289_,322,333 -- the ONLY mismatch is this one copy: thvm
288 vs WM 289.  THVM_ATP_COMM_DROP_DUP RE-AGES it one slot past its successor
(smallest batch key strictly greater than its own), landing it at WM's faithful
pick-289.  Gated on slot15 being live (re-derivation, not birth), so slot15 the rule
-- and its uniquely-parented pick-99 COMM copy -- stay intact; NOT a drop (a drop
would lose the slot15-class copy WM selects at 289, regressing to firstdiv=289).

CONFIG: THVM_ATP_CP_SIDE=1 THVM_ATP_FLAT_SUBSUME=1 THVM_ATP_COMM_REAGE=1
        THVM_ATP_COMM_DROP_DUP=1 THVM_ATP_WALDMEISTER=1
        bin/test_atp_wolfram_bench soa.pr 200000 20   (soa.pr = /tmp/soa.pr, the
        3-axiom form; WM ref /tmp/soa_align/wm.txt, 711 selections)
RESULT: firstdiv 288 -> 290, prefix 1..289 byte-identical, weights-exact,
        thvm-only=0, wm-only=0, pick-99 = COMM, all 15 slot15-class positions ==
        WM.  pick-288 = `x.(x.x)=y.(y.y)` (WM's 288), pick-289 = slot15 term (WM's
        289).  The next residual at 290 is a SEPARATE non-slot15 age inversion
        (`(x.(x.x)).y = y.y` vs `x.x = x.(y.(y.y))`, the 3-cycle noted below) --
        out of scope for the slot15 fix, needs a distinct mechanism.
CODE:   src/atp/_.c thvm_atp_generate_cps_wm DROP-DUP re-age block;
        atp_pair_is_slot15_term + atp_term_is_slot15_lhs/atp_term_is_slot15_rhs +
        atp_slot15_rule_is_live; src/thvm.h use_comm_drop_dup; setter
        thvm_atp_set_use_comm_drop_dup + THVM_ATP_COMM_DROP_DUP env in
        tests/test_atp_wolfram_bench.c.
KNOB OFF (default): byte-identical -- bin/test_atp 136232/136232; REAGE-only soa
        firstdiv still 288; ZeroIsAbsorbing@5000 unchanged ON and OFF (steps=546
        cps=1097 max_cps=1117 -- the detector never fires off-Sheffer, slot15's
        term is not a live rule there).

## RESOLVED 2026-06-21: THVM_ATP_COMM_REAGE re-ages the one early seq564 copy
The "global CP-formation reorder PROVEN impossible" verdict below is SUPERSEDED for
the 126-band: a SINGLE-CP batch-key re-age closes it.  The band picks 126..~286 is a
RIGID +1 SHIFT of ONE weight-120 copy (the seq564 sibling `(x.x).y = (x.y).y`, WM
cp877).  thvm forms it in rule13's (slot19's) OWN birth batch from rule13 x slot15
(the non-canonical comm-side equation WM never installs), where slot15's small
leaf-list rank keys it ONE slot too early (cp_seq 564 -> pick-125, ahead of the
rule13 x eqn-6 copy WM selects at 125).  THVM_ATP_COMM_DEFER over-corrected by
DELETING that copy (first seq564 then at pick-135).  THVM_ATP_COMM_REAGE (the
INVERSE) instead bumps that one CP's batch key just past the rule13 x eqn-6 copy so
it sorts one slot later (cp_seq 565 -> pick-126 = WM's cp877), re-zipping the whole
weight-120 tail.  eqn-9 (rule13's WM cp877 parent) is ABSORBED in thvm (not a live
rule), so FORCE-EMIT is unavailable; RE-AGE of the slot15-derived analog is the fix.

CONFIG: THVM_ATP_CP_SIDE=1 THVM_ATP_FLAT_SUBSUME=1 THVM_ATP_COMM_REAGE=1
        THVM_ATP_WALDMEISTER=1  bin/test_atp_wolfram_bench soa.pr 300 40
RESULT: pick-126 = seq564 sibling (= WM cp877), prefix 1..125 byte-identical,
        pick-99 = COMM, thvm-only=0, the band 126.. is a clean +1 shift of the
        comm-defer tail (ON[126:] == defer[125:] for the full traced tail).  The
        next residual is the separate 3-cycle at picks ~287/289/291.
CODE:   src/atp/_.c thvm_atp_generate_cps_wm re-age block (the comm-reage gate),
        atp_pair_is_seq564_sibling + atp_term_is_sq_dot_var/atp_term_is_dot_xy_dot_y
        + atp_eq_is_noncanonical_comm_side (slot15) + atp_rule_is_seq564_producer
        (rule13/slot19); src/thvm.h use_comm_reage; setter +
        THVM_ATP_COMM_REAGE env in tests/test_atp_wolfram_bench.c.
KNOB OFF (default): byte-identical -- bin/test_atp 136232/136232, soa firstdiv still
        125 (base two knobs) / 126 (+comm-defer); ZeroIsAbsorbing@5000 unchanged
        (proves steps=546 cps=1097, no AC-thrash -- reage is surgical, scoped to
        the rule13-producer x slot15 seq564-sibling pair).

## DEFINITIVE 2026-06-20: needs the global CP-formation reorder (local fix PROVEN impossible)

thvm's backward subsumption is ALREADY WM-faithful: atp_eset_subsume_by_new
(src/atp/_.c, ON in the WALDMEISTER preset, KBO_UN-gated, orphans victims, skips
oriented rules) == WM RueckwaertsSubsumierung (Interreduktion.c:251, syntactic
one-sided MO_TermpaarSubsummiertZweites under !TP_TermpaarIstRegel).  The
THVM_ATP_BWD_SUB knob (atp_push_rule:8675) is a DIFFERENT Vampire-style
over-firing mechanism -- the WRONG one, correctly OFF (it diverges soa @10).
pick-125 cannot be flipped locally: slot15 (C3 V0 (C3 V1 V0))=(C3 (C3 V1 V1) V0)
and eqn-10 (C3 V0 (C3 V1 V0))=(C3 V0 (C3 V1 V1)) share an LHS, differ by ONE
top-commutativity RHS swap; the plain subsumption predicate (correctly) bails.
Three WM-faithful comm-aware widenings that DO subsume slot15 all forked the soa
prefix at pick-99: removing slot15 suppresses formation of the commutativity CP
(C3 V0 V1)=(C3 V1 V0) that pick-99 selects, because thvm's slot15 is the UNIQUE
parent of that CP copy while WM produces an equivalent copy from a DIFFERENT live
parent at the right FIFO age.  => CP-formation-GRAPH divergence (which parent
pair yields the surviving duplicate-CP copy) + the cross-rule weight-120 FIFO-age
interleave = the global reorder.  No local subsumption/orientation predicate
fixes it.  (Verified workflow wf_5e7ce207.)

CONFIG (opt-in):
  THVM_ATP_CP_SIDE=1 THVM_ATP_FLAT_SUBSUME=1 THVM_ATP_WALDMEISTER=1
  bin/test_atp_wolfram_bench /tmp/soa.pr <budget> <maxw>
RESULT: prefix=124 identical=N firstdiv=125; thvm pop-set superset of WM's;
        weights-exact through the aligned prefix.

THE pick-125 FACT (thvm side):
  thvm selects at weight 120 in pure formation-age (cp_seq) FIFO order.
  Picks 123,124 = the equation copies cp_seq 562,563
    opCenterdot(opCenterdot(v1,opCenterdot(v1,v1)),v2) # opCenterdot(v2,v2)
    == (C3 (C3 V0 (C3 V0 V0)) V1) # (C3 V1 V1).
  Pick 125 = cp_seq 564
    (C3 (C3 V0 V0) V1) # (C3 (C3 V0 V1) V1)
    == opCenterdot(opCenterdot(v1,v1),v2) # opCenterdot(opCenterdot(v1,v2),v2).
  thvm's NEXT equation copy after 563 is cp_seq 593 (then 613); the seqs
  between (564 seq564-form, 565 a distinct w=120 CP) are NOT the equation.

DOES WM FORM seq=564?  YES -- the prior "appears NOWHERE in WM's trace"
claim is WRONG (it was a grep miss: WM splits lhs/rhs across the `#`
multiline form and renames vars).  GREP EVIDENCE (banked /tmp/soa_align):
  wm.txt:5602  critical pair 991 (parents -10 and 13):
       opCenterdot(opCenterdot(x1,x1),x2) # opCenterdot(x2,opCenterdot(x2,opCenterdot(x2,opCenterdot(x1,x2))))
    ... normalized to:
       opCenterdot(opCenterdot(x1,x1),x2) # opCenterdot(opCenterdot(x1,x2),x2)
    ... added to SUE: 120, 659.
  This normalized form == thvm seq=564 (alpha-equal).  WM forms it 12x
  (wm.txt:5604,6772,41084,41567,42364,42750,43244,43879,44475,45001,...),
  ALWAYS weight 120, first SUE age 659, later 5046/5206/5304/5454/9047.
  WM NEVER selects it (no "added as new rule" with this form): it sits
  in SUE forever, buried behind lower-age weight-120 copies.

ROOT CAUSE (decisively re-diagnosed; pure AGE-INTERLEAVE at weight 120):
  Both engines queue (a) the equation and (b) seq=564 at weight 120.
  The divergence is the RELATIVE FORMATION AGE of seq=564's first copy
  vs the equation's 3rd copy:
    WM : eqn ages 222, 511, 577 ; seq564 first age 659
         -> 3 eqn copies precede seq564 -> WM picks 123,124,125 = eqn,
            seq564 waits.
    thvm: eqn cp_seq 562, 563     ; seq564 cp_seq 564  ; next eqn 593
         -> only 2 eqn copies precede seq564 -> thvm picks 123,124 = eqn,
            125 = seq564.
  thvm is MISSING one equation copy in the (563, 593) age window that WM
  has at age 577.  WM forms ~33912 CPs and queues the equation 227x at
  spread-out ages (222/511/577/1213/...); thvm forms fewer, with the
  equation's copies landing at cp_seq 167/192/235/.../562/563/593/613.
  The first 124 weight-ordered selections coincide; at the 125th the
  per-copy age drift finally swaps a weight-120 selection.

THE TASK'S HYPOTHESIZED LEVER (pop-time AC-perm-subsume) IS A DEAD END --
three independent disproofs:
  1. seq=564 is NOT AC-redundant.  lhs (C3 (C3 V0 V0) V1) and rhs
     (C3 (C3 V0 V1) V1) are genuinely distinct terms (inner V0,V0 vs
     V0,V1); they are NOT a commutativity/AC permutation of each other.
     atp_cp_perm_subsumed correctly returns 0 on it.
  2. opCenterdot (Sheffer NAND) is NOT an AC operator.  auto_ac on the
     soa axioms reports label 3: C=0 A=0 (THVM_ATP_AUTO_AC_DEBUG ->
     mask=0x0).  The perm-subsume mask is empty; the WM-faithful filter
     is structurally inert here (run stats: perm-subsumed=0).
  3. Even if seq=564 were dropped, pick-125 would become cp_seq 565
     ((C3 V0 V0) # (C3 (C3 V1 (C3 V1 V1)) V0)) -- ANOTHER non-equation
     w=120 CP -- NOT the equation.  The equation's 3rd thvm copy is
     cp_seq 593, ~29 ages later.  No pop-time drop of seq=564 can
     promote the equation; only forming a 3rd equation copy in (563,593)
     would, which is the CP-FORMATION-ORDER subsystem, not a filter.
  Empirical confirmation: forcing the mask on (THVM_ATP_PERM_SUB=1
  THVM_ATP_PERM_SUB_MASK=3) drops just 1 unrelated commutativity CP
  (perm-subsumed=1) AND shatters the whole alignment (pick-123 jumps to a
  pri=224 CP, prefix 1..124 broken) -- the AC-saturation-on-a-non-AC-top
  blow-up MEMORY warns about.

VERDICT: DEFERRED -- CP-formation age/multiplicity class, NOT a filter
class.  WM's seq=564 (== thvm's) is a real shared CP both engines queue
at weight 120; the only difference is WM forms a 3rd equation copy (age
577) in the window where thvm forms none, so WM's seq=564 (age 659) loses
the pick-125 race that thvm's seq=564 (cp_seq 564) wins.  Closing soa past
124 requires matching WM's CP-FORMATION ORDER + duplicate MULTIPLICITY
(the atp_overlap_ij enumeration order producing one more equation copy in
the critical age window), exactly the subsystem the prior wmo_dfs verdict
isolated.  A pop-time perm-subsume is the WRONG TOOL: there is no
AC-redundant CP to drop here, and dropping seq=564 promotes seq=565, not
the equation.

================================================================================
A-vs-B RE-DIAGNOSIS (2026-06-20): NOT (A) missing-enumeration; NOT a
re-overlap either.  Pure intra-rule overlap-ORDER at weight 120.  The
"thvm forms only 2 E copies / MISSES the age-577 copy" premise above is
WRONG (it counted only the (563,593) window, ignoring 15 earlier E copies).
--------------------------------------------------------------------------------
THE THREE WM E-COPIES' PARENTS (CONFIRMED, /tmp/soa_align/wm.txt):
  age 222  CP287  parents (-7, -5)  -- E-eqn-7 [x.x = (z.(z.z)).x]
                                       x E-eqn-5 [commutativity]
  age 511  CP774  parents (11, -7)  -- rule 11 [x.(x.(z.x)) -> z.x]
                                       x E-eqn-7
  age 577  CP876  parents (13, -6)  -- rule 13 [x.(x.(x.z)) -> z.x]
                                       x E-eqn-6 [x.x = x.(z.(z.z))]
  All THREE are DISTINCT (parents,pos) superpositions, each fired ONCE in
  WM; the age-577 copy is a fresh superposition, NOT a re-overlap of
  222/511.  (WM's seq564-equiv = CP991 parents (-10, 13) age 659.)

THVM ENUMERATES ALL THREE -- including the exact age-577 (rule13 x eqn6):
  thvm slot map by TERM (THVM_ATP_RULE_TRACE): slot19 == WM rule13
  [(C3 V0 (C3 V0 (C3 V0 V1)))->(C3 V1 V0)]; slot7 == WM eqn6-reversed
  [(C3 V0 (C3 V1 (C3 V1 V1)))->(C3 V0 V0)]; slot5 == commutativity;
  slot8 == E-as-rule [(C3 (C3 V0 (C3 V0 V0)) V1)->(C3 V1 V1)].
  THVM_ATP_CPGEN_DEBUG (parent-TERM provenance) + THVM_ATP_CP_FORM_TRACE
  interleaved (/tmp/soa_align/thvm_both.txt) pin the heap copies:
    cp_seq 562 = slot19 x slot8  (rule13 x E-rule)         -> E
    cp_seq 563 = slot19 x slot7  (rule13 x eqn6)           -> E  <== the
                  EXACT analog of WM age-577 (rule13 x eqn6); thvm forms
                  it at 563, NOT in (563,593), NOT zero.
    cp_seq 564 = slot19 x slot15/20 (rule13 x other)       -> seq564
    cp_seq 593 = slot20 x slot3                            -> E
  thvm produces E from ~95 distinct (parent-pair) overlaps in the budget-700
  cpgen; it OVER-produces E vs WM (18 heap copies by cp_seq 593 vs WM's 3
  by age 659).  There is NO superposition WM enumerates that thvm forms
  zero times.  => the task's case (A) is FALSE.

THE REAL DIVERGENCE (rule-13 / slot-19 internal overlap ORDER):
  WM fires rule13's overlaps spread out: rule13 x eqn6 -> E at age 577;
  rule13 x (-10) -> seq564 at age 659.  So WM's seq564 lands 82 ages AFTER
  its 3rd E copy -> WM picks E at 125.
  thvm fires slot19's overlaps consecutively at slot19's birth (rules=22):
  slot19 x slot8 -> E (562), slot19 x slot7 -> E (563), slot19 x slot15 ->
  seq564 (564).  So thvm's seq564 lands 1 age AFTER its (locally) 2nd E copy
  -> thvm picks seq564 at 125.
  Both engines enumerate the identical overlap SET; they differ only in the
  RELATIVE EMISSION ORDER of one rule's (rule13/slot19) E-overlaps vs its
  seq564-overlap, and in WM interleaving the seq564 copy from a DIFFERENT
  parent (-10,13) at a much later age (659) than thvm's same-rule slot19
  emission (564).

VERDICT (FINAL): NOT (A) missing-enumeration (thvm forms the age-577
rule13xeqn6 overlap at cp_seq 563), NOT (B) re-overlap-multiplicity (WM's
three E copies are three distinct one-shot superpositions, none repeated).
The residual is the atp_overlap_ij / cp_visit EMISSION-ORDER within a
single rule's overlap batch: thvm emits rule13's two E-overlaps (x slot8,
x slot7) before its seq564-overlap (x slot15), so seq564 immediately
follows the E pair at the same rule-birth tick; WM's heap-age assignment
interleaves a non-rule13 seq564 producer (-10,13) far later.  Matching
pick-125 requires reproducing WM's per-rule CP emission order + WM's
cross-rule age interleave at weight 120 -- the CP-FORMATION-ORDER subsystem,
unchanged conclusion (DEFERRED) but with the precise mechanism now pinned:
it is an overlap-emission-ORDER question, not a missing CP and not a filter.

REGRESSION (this re-diagnosis is documentary; no source change):
  git diff empty; bin/test_atp green 136232/136232; pick-seq for soa
  prefix 1..124 unchanged (no code touched).

================================================================================
SLOT15-vs-EQN10 FORK RESOLVED (2026-06-20): CASE (ii) OVER-ENUMERATION, and
the only mechanism that flips pick-125 to E is GLOBAL (trades the prefix).
--------------------------------------------------------------------------------
THE FORK (was: is thvm's seq564 partner slot15 == WM's seq564 partner eqn-10?).
  thvm seq564 producer (cp_seq 564, FIRST copy; pinned by [cpgen] norm-match,
  thvm_both.txt:1687): outer=slot19[=WM rule13] x inner=slot15, combo=0.
  thvm slot15 = (C3 V0 (C3 V1 V0)) -> (C3 (C3 V1 V1) V0)        [unorientable]
  WM eqn-10   = (C3 V0 (C3 V1 V0))  = (C3 V0 (C3 V1 V1))        [wm.txt:5129]
  SAME LHS, DIFFERENT RHS.  They are NOT the same equation.  thvm's slot20
  (= (C3 V0 (C3 V1 V0)) -> (C3 V0 (C3 V1 V1)), thvm_rt slot20) IS eqn-10.
  => CASE (ii): thvm forms seq564 from rule13 x slot15, a (parent-pair) WM
     never performs -- WM has NO slot15-as-rule.

WHY thvm has slot15 as a rule and WM does not (the over-enum source):
  WM NEVER orients slot15's term into a rule and NEVER numbers it in E:
  at wm.txt:5684 WM TOUCHES the slot15-term CP
  `opCenterdot(x1,opCenterdot(x2,x1)) # opCenterdot(opCenterdot(x2,x2),x1)`
  and reports "... subsumed." -- WM joins/subsumes it (via eqn-10 added at
  5129 + commutativity) so it never enters R or E.  thvm instead SELECTS it
  (pick-121) and installs slot15 as an active oriented rule, then overlaps it.
  ORDER DEPENDENCE: thvm derives slot15 (thvm_rt slot15) BEFORE slot20/eqn-10
  (thvm_rt slot20); at slot15's birth eqn-10 does not exist so thvm cannot
  subsume it.  WM derives eqn-10 first and uses it to kill the slot15-term.

NO LOCAL FILTER (pop-time drop of seq564 promotes seq565, not E -- already
established).  And the 17-E-copies recount: thvm forms E at cp_seq 167/192/
235/236/238/246/270/293/316/340/396/436/475/493/540/562/563/[564=seq564]/593...
(17 E copies BEFORE 564, not 2) and SELECTS E at picks 14/17/24/25/26/28/31/
34/37/40/.../123/124.  pick-125 is therefore a PURE weight-120 FIFO interleave
over the WHOLE run's formation-age stream, not a 2-vs-3 local count.

THE ONLY KNOB THAT YIELDS E@125 IS GLOBAL (decisive empirical trade proof,
WALDMEISTER preset, /tmp/soa.pr 300 40, real-selection stream excl. ORPHAN):
  baseline                 pick125 = seq564   prefix1..124 baseline
  THVM_ATP_BWD_SUB=1       pick125 = E (!)    but FIRST DIVERGES AT pick-10
                                              (pri 48 vs 109) -- whole stream
                                              reshuffled; picks 118..124 all
                                              differ.  This IS WM's backward
                                              subsumption (the mechanism that
                                              removes slot15-term + collapses
                                              E-dups); it reproduces WM's
                                              pick-125 ONLY by reproducing WM's
                                              entire weight-120 formation order.
  THVM_ATP_BWD_DEMOD=1     pick125 = seq564   prefix SAME  (no effect on 125)
  THVM_ATP_BWD_GROUND_JOIN=1 pick125 = seq564 prefix SAME  (no effect)
  THVM_ATP_FWD_SUB=1       pick125 = seq564   prefix SAME  (no effect)
  THVM_ATP_ESET_SUBSUME=0  diverges @10       THVM_ATP_WM_DEMOTE=0 diverges @10
  THVM_ATP_EMISSION_ORDER=0 diverges @16   THVM_ATP_POP_SUBSUME=0 diverges @16
  THVM_ATP_IMPLICIT_CP=1   pick125 != E, prefix DIFF (other trajectory)

CONCLUSION (DEFERRED, with the global trade now PROVEN): every prefix-
preserving toggle leaves pick-125 = seq564; the single toggle that lands E at
125 (BWD_SUB = WM's RueckwaertsSubsumierung) reorders the selection stream from
pick-10 onward.  Flipping soa-125 = forming WM's seq564 from eqn-10 x rule13 at
WM's late age = adopting WM's backward-subsumption-driven formation order
WHOLESALE, which trades the 1..124 match for the 125 match.  No baseline-
preserving local fix exists; the residual genuinely requires the global CP-
formation-ORDER rewrite (matching WM's per-rule emission order AND its
backward-subsumption-driven cross-rule weight-120 age interleave).  Same shape
would help Meredith only via that same global reorder, not a local gate.

REGRESSION (this resolution is documentary; no source change):
  git diff empty; bin/test_atp green 136232/136232; SKIToBCKW c1 @1868 intact
  (pick-1868 = (C10 C8 (C10 C7 C5))->..., C8/W head); all baseline pick-seq
  md5 unchanged by construction (no code touched).

================================================================================
EQN-4 ANGLE RE-OPENED (2026-06-20): thvm forms+selects eqn-4 EARLY and FAITHFULLY;
the prior "thvm orients eqn-2 / forms eqn-4 late / COMM from slot-pairs" claim is
FALSE on every point.  eqn-4 is NOT the firstdiv=125 cause -- a brief-level conflation.
--------------------------------------------------------------------------------
WM eqn-4 = (C3 (C3 V0 V0) V1) = (C3 V1 (C3 V0 V0))  [commute-the-squared-subterm]
  WM: CP77 parents (2, -2) = rule2 x eqn-2, weight 120 age 65 (wm.txt:484); kept
      as UNORIENTED EQ (wmfacts ElternNr=4294967292 mono=0); selected as equation 4;
      ue(-4, 1) [eqn-4 x rule1] -> COMM, added to E as mono equation 5 (wm.txt:1228);
      COMM then BACK-SUBSUMES eqn-4 (wm.txt:1238 "subsumes equation no. -4; removed").

THVM (THVM_ATP_CPGEN_DEBUG / RULE_TRACE+ORIENT_KBO_CHECK / CP_PICK_TRACE on
/tmp/soa.pr; banked /tmp/soa_align/cpgen_eqn4.txt + pick_trace.txt):
  FORMS eqn-4 EARLY (cp_seq ~61): cpgen norm `C3(x0,C3(x1,x1)) = C3(C3(x1,x1),x0)`
    (alpha-equal to WM eqn-4, side-swapped) at lines 121/149/333, from parent pairs
    slot3 x slot1, slot4 x slot2, slot6 x slot2.  slot2 = the soa 3rd axiom (eqn-2);
    slot4 x slot2 IS the rule x eqn-2 analog of WM's (2,-2).  thvm OVER-produces eqn-4
    copies vs WM's single CP77 (same over-production as the weight-120 equation).
  KEEPS eqn-4 UNORIENTED (faithful): RULE_TRACE shows
    `RULE 6 ... C3(x_0,C3(x_1,x_1)) -> C3(C3(x_1,x_1),x_0)  (unorientable)`,
    r_orient=0, n_unorient++; ORIENT_KBO_CHECK does NOT fire (thvm_kbo_naive agrees
    UN).  KBO reason: lex tiebreak hits V0 (var) vs C3(V1,V1) (non-containing term)
    = incomparable.  Matches WM mono=0.  (eqn-2 ALSO unoriented -- prior fact holds.)
  SELECTS eqn-4 at pick-8 (pri=120): `lhs=(C3 V0 (C3 V1 V1)) rhs=(C3 (C3 V1 V1) V0)`.
  DERIVES COMM at pick-9 (pri=48): cpgen line 255 `outer=slot4[eqn-4] x inner=slot1`
    (+ lines 265/267/299/301/303 from slot4 x slot2/slot4/slot0) -> `C3(x0,x1)=C3(x1,x0)`.
    => COMM is sourced FROM eqn-4 (slot4/slot6), NOT "from slot-pairs not eqn-4".
  BACK-SUBSUMES eqn-4 after COMM (faithful): after COMM (RULE 5 trace 132) thvm runs
    `ESET-SUBSUME kill (slot 4, trace 132) lhs=(C3 V0 (C3 V1 V1)) rhs=(C3 (C3 V1 V1) V0)`
    = COMM removing eqn-4, == WM's "subsumes equation no. -4".

LIFECYCLE PARITY (thvm <-> WM), byte-faithful:
  form early (cp_seq 61 / CP77) -> keep unoriented (r_orient=0 / mono=0) -> select
  (pick-8 / eqn-4) -> overlap x ax1 -> form COMM -> select COMM (pick-9 / eqn-5) ->
  COMM back-subsumes eqn-4 (ESET-SUBSUME kill / "equation -4 removed").

WHY THE BRIEF'S WORRY IS A NON-ISSUE (the conflation):
  pick-99 = COMM (the recurring weight-48 commutativity copy); pick-125 = seq564
  `(C3 (C3 V0 V0) V1) = (C3 (C3 V0 V1) V1)` -- inner V0,V1 -- which is NOT eqn-4
  (eqn-4 RHS = (C3 V1 (C3 V0 V0))).  thvm's canonical eqn-4 copy lands at pick-127
  (seq=582 `(C3 (C3 V0 V0) V1)=(C3 V1 (C3 V0 V0))`).  The eqn-4 -> COMM path is the
  pick-8/9 EARLY event and is faithful; firstdiv=125 is the SEPARATE weight-120
  equation/seq564 FIFO-age interleave already pinned above.  No new local lever from
  the eqn-4 angle -- the early eqn-4 -> COMM is already what WM does.

VERDICT (eqn-4 angle): CLOSED.  thvm forms+selects+unorients+back-subsumes eqn-4
faithfully and EARLY; the prior agent's eqn-2-orientation / late-eqn-4 / slot-pair-COMM
explanation is refuted on all three counts.  firstdiv=125 remains the global weight-120
CP-formation-ORDER residual (seq564 vs the weight-120 equation), unchanged DEFERRED.

REGRESSION (documentary; no source change):
  git diff empty; bin/test_atp green 136232/136232; SKIToBCKW c1 @1868 intact
  (pick-1868 = (C10 C8 (C10 C7 C5))->..., C8/W head); pick-99 = COMM, pick-125 =
  seq564 unchanged; all baseline pick-seq md5 unchanged by construction (no code).

================================================================================
COMM-SUBSUME IMPLEMENTED + DIRECTLY DISPROVEN (2026-06-21): the scoped
top-commutativity eset-subsume DOES drop slot15 exactly as WM does, and it
STILL forks soa at pick-99 (worse than 125) AND explodes a commutative-ring
baseline.  THIRD independent direct disproof of the local-subsume lever.
--------------------------------------------------------------------------------
IMPLEMENTED (gated THVM_ATP_COMM_SUBSUME, DEFAULT OFF; knob OFF byte-identical,
bin/test_atp 136232/136232 green):
  atp_eq_subsumes_pair_mod_top_comm (src/atp/_.c) widens atp_eset_subsume_by_new
  to also drop an older equation whose LHS matches the new equation's LHS and
  whose RHS equals the new RHS modulo ONE top-`.`-child-swap (a.b == b.a),
  gated on a live `f(x,y)=f(y,x)` equation (atp_op_is_live_commutative).  This
  is precisely WM's commutativity-normalized Interreduktion -- NOT AC-saturation
  (single top swap, no equivalence-class enumeration).

CONFIRMED IT DROPS slot15 EXACTLY AS WM DOES (RULE_TRACE, knob ON):
  ESET-SUBSUME kill (slot 15, trace 418) lhs=(C3 V0 (C3 V1 V0)) rhs=(C3 (C3 V1 V1) V0)
  i.e. eqn-10 `x.(y.x)=x.(y.y)` (added later) drops slot15 `x.(y.x)=(y.y).x` the
  moment it enters, one top-`.`-swap apart -- the over-enumeration source named
  in the SLOT15-vs-EQN10 section above.  10 ESET kills total vs OFF's 2.

RESULT: soa firstdiv 125 -> 99 (WORSE), NOT a blow-up (pick count 32110 ~ base):
  knob OFF: picks 97,98,99 are ALL COMM `x.y=y.x` (pri=48, seq 581/599/632) ->
            pick-99 = COMM, aligned with WM.
  knob ON : pick 97,98 = COMM (581,627); the THIRD COMM copy is GONE.  pick-99
            now lands a pri=65 CP (seq 591); the displaced w=120 CPs at that age
            are ORPHANs tagged `pa=418(dead)` -- their parent IS the just-killed
            slot15.  thvm loses the third COMM copy because slot15 is its UNIQUE
            parent for that copy (CPGEN: `outer=slot15[x.(y.x)->(y.y).x]
            inner=slot5[x.y->y.x COMM] pos=(root) combo=2`).  WM keeps its third
            COMM copy from a DIFFERENT live parent (eqn-7/eqn-6 pairs, the
            age-577 class) at the right FIFO age.  => dropping slot15 is correct
            but its uniquely-parented COMM copy dies with it; matching WM needs
            forming that COMM copy from WM's alternate parent at WM's age = the
            GLOBAL CP-formation reorder, not any subsumption predicate.

EXPLOSION on a NON-soa baseline (the AC-thrash MEMORY warns of, now demonstrated):
  CommutativeRingAxioms__ZeroIsAbsorbing: OFF proves steps=546 cps=1097; ON does
  NOT prove (=> RUNNING, timeout) and blows to cps=383752 (350x), rules=9386.
  At a 5000-step cap it does 910 ESET kills (vs OFF's 1): the comm widening
  remove-and-rederive thrashes on the commutative `.`-over-`+` ring, killing
  `(x+x).y=x.(y+y)`-class equations and their commutative variants in lockstep
  with rising re-derivation traces (1317..1792).  The single-top-swap, justified
  only by "a live commutativity equation exists", is NOT surgical: any
  commutative operator that interacts with a second operator cascades.

SHORT-BASELINE SANITY (knob ON): 7/8 byte-identical (inert) -- GroupAxioms
  LeftIdentity/LeftInverse/InverseOfInverse, BooleanAxioms And/OrCommutativity,
  DoubleNegation, AndIdempotence all PROVED with identical steps/rules/cps.
  ZeroIsAbsorbing is the one that explodes (above).

VERDICT (FINAL, DEFERRED -- third direct disproof): the lever the task
hypothesized (a scoped comm-aware backward subsumption that drops slot15 like
WM) is now IMPLEMENTED and MEASURED.  It is correct in isolation (drops slot15
byte-for-byte as WM, knob OFF inert, suite green) but (a) forks soa to 99
because thvm's slot15 is the UNIQUE parent of the pick-99 COMM copy whereas WM's
copy comes from a different live parent, and (b) explodes a commutative-ring
baseline via remove-and-rederive AC-thrash.  No baseline-preserving local
subsumption can both drop slot15 and preserve its uniquely-parented COMM copy;
that requires matching WM's CP-formation graph + FIFO-age interleave globally.
Knob retained DEFAULT OFF as the faithful WM mechanism whose isolated
correctness pins the global-reorder dependency.
