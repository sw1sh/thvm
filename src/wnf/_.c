// wnf - reduce a term to weak normal form.
//
// STUB: full stack-machine reducer (with one-interaction-per-file
// dispatch into src/interact/) lands in step 6. For now this is a
// no-op so the rest of the runtime (and the test harness) can compile
// and link.
fn Term wnf(Term t) {
  return t;
}
