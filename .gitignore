bin/
*.o
*.a
*.dylib
*.so
.DS_Store
.claude/

# Compiled WL paclet library (rebuilt by `make wl`).
wl/THVMLink/LibraryResources/

wl/Notebooks

# Local-only reference symlink to the research prototype. Create with:
#   ln -s ../TinyHVM TinyHVM
TinyHVM
waldmeister
tilelang

build/

# Hackathon-prep WIP. Untrack selectively when ready.
bench/metal-problems/
external/mlx/

# Python bytecode (py/ ctypes wrapper)
.venv
__pycache__/
*.pyc

# Built dylib for py/ ctypes wrapper (rebuild via `make py`)
py/thvm/libthvm_py.dylib

# brain/ is now tracked as a submodule (github.com/sw1sh/brain-arc, private).
# Local runs/data/.venv inside the submodule remain ignored via brain/.gitignore.

# Regenerable Vampire benchmark artifacts (re-export via run_vampire_baseline.sh)
tools/baselines/vampire_raw/
tools/baselines/vampire_tptp/
