bin/
*.o
*.a
*.dylib
*.so
.DS_Store
.claude/

# Compiled WL paclet library (rebuilt by `make wl`).
wl/THVMLink/LibraryResources/

# Built documentation notebooks (regenerate with
#   wolframscript -f wl/THVMLink/build.wls
# ).
wl/THVMLink/Documentation/
wl/THVMLink/ResourceDefinition.nb

wl/Notebooks

WolframParser

# Local-only reference symlink to the research prototype. Create with:
#   ln -s ../TinyHVM TinyHVM
TinyHVM
waldmeister
tilelang
brain

build/

# Hackathon-prep WIP. Untrack selectively when ready.
bench/metal-problems/
external/mlx/

# Python bytecode (py/ ctypes wrapper)
.venv
__pycache__/
*.pyc

# Built dylib for py/ ctypes wrapper (rebuild via `make py`)
py/thvm/libthvm_py.dylib

# Regenerable Vampire benchmark artifacts (re-export via run_vampire_baseline.sh)
tools/baselines/vampire_raw/
tools/baselines/vampire_tptp/

# Transient sweep-output artifacts.  parity_perstep_partial.tsv is the
# incremental writer's per-row dump from diff_proofobject.wls (overwritten
# by the final parity_perstep.tsv); compare_methods_batch.tsv is a
# cross-method TSV regenerated by the comparator runs.
tools/baselines/parity_perstep_partial.tsv
tools/baselines/compare_methods_batch.tsv
